Declare the `slurpit-sdk` and `prometheus-client` SDKs in the `service` package extra, and `ipfabric` in a new, separate `ipfabric` extra (its `niquests` dependency would otherwise replace `urllib3` for every adapter in the default Sync image), and pass each SDK's `verify` setting under the name it actually accepts, so the bundled IP Fabric, Slurp'it, and Prometheus adapters can load and connect in a standard deployment.
