A planned operation is now written by exactly one destination mutation — the convergent upsert that
already carried the plan's cardinality-many peer list — and the second, targeted relationship write
that followed it was removed after a live test proved the upsert alone replaces and empties a peer
set.
