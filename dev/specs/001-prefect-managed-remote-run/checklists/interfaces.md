# Interfaces, Operability & Verification Checklist: Prefect-Managed Remote Infrahub Sync Run

**Purpose**: Validate that the spec's interface contracts (execution request, RunResult, flow parameters, configuration directory, log forwarding), example operability requirements, and success-criteria measurability are complete, precise, and objectively verifiable as written. These items test the requirements document, not the implementation.
**Created**: 2026-07-30
**Feature**: [spec.md](../spec.md) — brief authority: DB-001 v2 (`batch-developer-preview`), card LOCAL-DP-001

**Note**: This checklist is generated by the `/speckit-checklist` command based on feature context and requirements.
**Review**: Independently evaluated 2026-07-30 by the requirements reviewer; remediations recorded inline. Code grounding: `infrahub_sync/cache/paths.py` (`run_id` format, run-dir layout), `infrahub_sync/cache/parquet_io.py` and `potenda/__init__.py` (`plan.parquet` writer, one row per element), `infrahub_sync/utils.py` (per-call config re-resolution), `tests/` (targeted CLI test population).

## Execution Request Contract Quality

- [x] CHK001 Are all four execution-request fields specified with type, optionality, and default (`operation` default `plan` is stated; are `confirm_writes` and `branch` defaults equally explicit)? [Completeness, Spec §Key Entities Execution request]
  > REMEDIATED: `confirm_writes` defaults to `false`; `branch` omission yields today's CLI-default behavior (configuration branch setting, else Infrahub server default).
- [x] CHK002 Is the semantics of the optional `branch` parameter defined — which system it addresses (Infrahub destination? source?), and what branch is used when it is omitted? [Gap, Spec §Key Entities Execution request]
  > REMEDIATED: `branch` is the Infrahub branch, forwarded exactly as the CLI `--branch` option today; omission semantics stated; nonexistent branch → `RunExecutionError`.
- [x] CHK003 Is the allowed value space of `sync_name` defined (a "logical configuration name" — character set, matching rule against the directory contents), so the boundary between a valid name and a refused value is derivable from the spec? [Clarity, Spec §Key Entities / §DBR-005]
  > REMEDIATED: `sync_name` is an opaque name compared by exact string equality against installed configuration names; any non-matching value is refused (`RunValidationError`). No character-set rule is needed because the value is never used to construct a path.
- [x] CHK004 Is the resolution mechanism from `sync_name` to a configuration specified (match on the config's `name` key, on a subdirectory name, or otherwise), or is "selected by logical name from one server-configured directory" left mechanically ambiguous? [Ambiguity, Spec §Key Entities Sync configuration]
  > REMEDIATED: resolution matches the `name` field of each `config.yml` discovered recursively under the configured directory — the same lookup the CLI `--name`/`--directory` path performs today (`utils.get_instance`).
- [x] CHK005 Is the list of pinned engine options exhaustive and anchored ("today's CLI defaults" — is "today" fixed to a specific commit/baseline so the pins are verifiable later)? [Measurability, Spec §Clarifications / §Key Entities]
  > REMEDIATED: "today's CLI defaults" is now anchored to the CLI option defaults at baseline commit `9edc1bc` in both the Clarification and Key Entities.
- [x] CHK006 Is "progress display disabled" on remote runs specified with its observable consequence (what disappears from output/logs), so its presence or absence can be checked? [Clarity, Spec §Clarifications]
  > REMEDIATED: defined as equivalent to `--show-progress false` — no progress-bar rendering appears in remote output or forwarded logs.

## RunResult Contract Quality

- [x] CHK007 Does every one of the seven `RunResult` fields carry both a type and an unambiguous meaning, and is the field set stated as exact (no extra fields permitted)? [Completeness, Spec §Key Entities RunResult]
  > REMEDIATED: an explicit "exactly these seven fields and no others" statement now follows the table.
- [x] CHK008 Is the `status` vocabulary exhaustively mapped to outcomes (plan-with-changes → `planned`, sync-that-writes → `applied`, either-with-no-changes → `no-change`), leaving no operation/outcome combination without a defined status? [Completeness, Spec §Key Entities RunResult]
  > Verified exhaustive; failures raise and return no result, so successful outcomes are fully covered.
- [x] CHK009 Are the cross-field invariants between `changed`, `status`, and `summary` stated (e.g., must `changed=false` coincide with `status="no-change"` and an all-zero `summary`?), or could a consistent-looking result be internally contradictory under the spec as written? [Gap, Spec §Key Entities RunResult]
  > REMEDIATED: invariants stated — `changed` is true iff `status != "no-change"` iff summary counts sum > 0; `planned` only with `operation="plan"`, `applied` only with `operation="sync"`.
- [x] CHK010 Is `run_id` defined with enough precision ("Infrahub Sync cache/run identifier") to assert on in DBA-010's result-schema tests — format, uniqueness, or at least its authoritative source? [Clarity, Spec §Key Entities RunResult]
  > REMEDIATED: defined as the allocated cache run identifier (today the sortable `YYYYMMDDTHHMM-<8 hex>` format) equal to the final path segment of `artifact_path`.
- [x] CHK011 Is the content of `artifact_path` specified — which "ordinary Sync artifacts" must exist there for each operation — so DBA-003's "canonical plan summary/artifact" evidence has a defined target? [Clarity, Spec §Key Entities RunResult / §DBA-003]
  > REMEDIATED: `artifact_path` is the run's cache directory containing at least `run.json` and `plan.parquet` for both operations; `plan.parquet` is the defined DBA-003 target.
- [x] CHK012 Is `RunResult` immutability stated as a requirement of the contract (the brief says "one immutable result") in a form that planning can enforce and tests can check? [Completeness, Spec §Key Entities RunResult]
  > REMEDIATED: fields cannot be reassigned after construction; DBA-010's result-schema assertions verify it.
- [x] CHK013 Is the canonical plan fingerprint definition complete and deterministic — field list, sort key, serialization, encoding, exclusions all fixed — and does the sort key (`resource`, `source_id`, `action`, `attribute`) provably order every plan row totally (no tie leaving row order, and thus the digest, undefined)? [Measurability, Spec §Key Entities / §Clarifications]
  > REMEDIATED: the row's full serialized form is now the final tie-breaker (with the current writer emitting one row per element, `source_id` is unique within `resource`, so ties cannot occur today); definition otherwise unchanged and still PROVISIONAL.
- [x] CHK014 Are the fingerprint's artifact references consistent — the Clarification names `plan.parquet` fields while Key Entities says "from the plan artifact" — is the artifact filename/format fixed in one place? [Consistency, Spec §Clarifications / §Key Entities]
  > REMEDIATED: Key Entities now names "the plan artifact `plan.parquet` in the run directory", matching the Clarification.

## Flow Parameter Typing & Validation Locus

- [x] CHK015 Is "typed flow parameters" (DBR-003) specific about where each validation happens — the invalid-`operation` edge case requires rejection by Prefect parameter validation before the flow body, while `sync_name` resolution and `confirm_writes` refusal happen inside the surface — is this split stated consistently everywhere those failures are described? [Consistency, Spec §DBR-003 / §Edge Cases / §User Story 3]
  > REMEDIATED: a "validation locus" statement in Key Entities fixes the split (`operation` membership → Prefect parameter typing before the flow body; `confirm_writes` gating and `sync_name` resolution → inside the shared surface as `RunValidationError`), consistent with the edge cases and Story 3.
- [x] CHK016 Do the flow parameters and the execution-request contract fields correspond one-to-one, with the spec stating that future contracts extend rather than fork this one? [Consistency, Spec §DBR-003 / §Key Entities]
  > REMEDIATED: "the flow parameters correspond one-to-one to these four fields; no additional remote parameter exists" added; extend-not-fork sentence already present.

## Configuration-Directory Mechanism

- [x] CHK017 Is the `INFRAHUB_SYNC_CONFIG_DIRECTORY` mechanism fully specified — exact variable name, read timing (serve start), no default, and missing/non-directory failing at serve start rather than per-run? [Completeness, Spec §Key Entities Sync configuration / §Clarifications]
  > Verified, plus the serve-start failure is now observable (process exits with an error naming the variable before any deployment is served).
- [x] CHK018 Is behavior defined for the directory's contents changing after serve start (configs added, removed, or edited between runs) — re-resolved per run or frozen at startup? [Gap, Spec §Key Entities Sync configuration]
  > REMEDIATED: directory path fixed at serve start; contents re-resolved per run (matching today's per-call lookup in `utils.get_all_sync`).
- [x] CHK019 Is what constitutes an installed configuration in that directory defined (YAML config plus generated models, per Key Entities), so "manually installed" is actionable in the example's runner-setup instructions? [Clarity, Spec §Key Entities / §In Scope]
  > REMEDIATED: sharpened to "a directory containing the project's `config.yml` plus its generated Python models".

## Log Forwarding & Observability

- [x] CHK020 Is the log-forwarding mechanism specified precisely (handler bridging the `infrahub_sync` logger hierarchy into the Prefect run logger, attached before the execution-surface call and removed after, independent of operator-set Prefect logging environment variables)? [Completeness, Spec §Clarifications / §In Scope]
  > Verified: fully specified in the Clarification and In Scope.
- [x] CHK021 Is "Infrahub Sync lifecycle log messages" defined well enough to serve as SC-002's denominator — which log lines count as lifecycle lines, and at what level — so "100% observable" is computable rather than judged? [Measurability, Spec §SC-002 / §DBR-012]
  > REMEDIATED: SC-002 now defines the denominator — every log record emitted under the `infrahub_sync` logger hierarchy at the run's effective level (INFO and above by default) between flow start and completion.
- [x] CHK022 Are log-forwarding requirements consistent with secret hygiene — DBR-012's "without exposing credentials" and DBA-008's canary scan over Prefect-visible logs reference the same redaction obligation? [Consistency, Spec §DBR-012 / §DBA-008]
  > Verified; the Failure contract now names the single redaction obligation covering exception messages and forwarded logs, which DBA-008's canary scan verifies.

## Example README Operability

- [x] CHK023 Are the README's required content elements enumerated and complete for the walkthrough it must support — install the extra, author/load the `InfraDevice(name, type)` schema, configure the runner (config directory, credentials), start Prefect, serve the flow, remotely invoke a plan, inspect the run? [Completeness, Spec §User Story 5 / §DBR-013 / §R-3]
  > Verified: User Story 5 enumerates all seven walkthrough elements; scenario 2 and In Scope now enumerate the content requirements.
- [x] CHK024 Does any requirement oblige the example to carry the trusted-development-environment caveat (Constraints: the default server "must not be documented as safe for public internet exposure"), or is that constraint unowned by any deliverable? [Gap, Spec §Constraints / §User Story 5]
  > REMEDIATED: the constraint now assigns ownership to the example README, and Story 5 scenario 2 plus the In Scope example bullet require the caveat's presence.
- [x] CHK025 Is "without reading package source" (DBA-011) operationalized by SC-009's "zero references to package source", and is the walkthrough's clean-context condition (a developer "with no prior exposure") defined enough to stage the verification? [Measurability, Spec §DBA-011 / §SC-009]
  > Verified: SC-009 operationalizes DBA-011 (README-only, zero source references, no prior exposure); DBA-011's evidence cell requires the clean-context walkthrough.
- [x] CHK026 Are the README's stated machine prerequisites required to be explicit (Python version, Prefect 3.7.2, reachable Infrahub instance), and is "no real credentials" in example content paired with a stated placeholder convention? [Completeness, Spec §User Story 5 / §Constraints]
  > REMEDIATED: Story 5 scenario 2 now requires explicit prerequisites (supported Python version, pinned Prefect version, reachable Infrahub with the qualified schema) and obviously-fake placeholder credentials.
- [x] CHK027 Are the "remote request examples" specified as to form — which Prefect REST endpoints/payloads they must demonstrate — so DBA-008's scan of "example request bodies" has a defined corpus? [Clarity, Spec §User Story 5 / §DBA-008]
  > REMEDIATED: the examples must demonstrate at least deployment flow-run creation and run-state/log inspection through Prefect's REST API, and these request bodies are declared the DBA-008 scan corpus.

## Measurability of Success Criteria & Evidence

- [x] CHK028 Is SC-001's "receive a run identifier immediately" quantified or reducible to an objective check (synchronous API response), or is "immediately" an unmeasured latency claim? [Ambiguity, Spec §SC-001]
  > REMEDIATED: "immediately" replaced with "in the synchronous response to the run-creation request, before the run executes".
- [x] CHK029 Does each DBA row's "verification evidence expected" name an inspectable artifact type (transcript, flow-run record, negative test, canary scan, walkthrough) such that "inspectable passing evidence" in Completion Conditions is achievable for all eleven criteria? [Measurability, Spec §Acceptance Criteria / §Completion Conditions]
  > Verified: all eleven rows name inspectable artifact types.
- [x] CHK030 Is the set of "existing targeted tests for CLI `diff` and `sync --no-parallel`" (DBA-009) identified or identifiable from the spec, so passing "through the shared execution surface" has a defined test population? [Ambiguity, Spec §DBA-009]
  > REMEDIATED: an Assumptions informed-default now names the population (`tests/test_cli_full_extract.py`, `tests/test_cli_parallel.py` serial case, `tests/cache/test_cli_sync_cache.py` serial case, CLI checks in `tests/test_logging.py`) from the `9edc1bc` baseline.
- [x] CHK031 Is the "reset copies of the qualified fixture" procedure (DBA-009, SC-007) defined — what is reset (destination objects, cache/run state, artifacts) and to what state — so the paired CLI-versus-flow comparison is reproducible? [Gap, Spec §DBA-009 / §SC-007]
  > REMEDIATED: defined in the Qualified demonstration fixture entity — destination returned to zero `InfraDevice` objects before each compared run, fresh `run_id` per run (no cache reuse), MockDB source unmodified.
- [x] CHK032 Is "without importing or contacting the orchestrator" (SC-006) paired with an observation method (import-graph assertion, network capture) or at least required to be evidenced, not asserted? [Measurability, Spec §SC-006 / §DBA-001]
  > REMEDIATED: SC-006 now names the method — an environment with the orchestrator package absent (imports fail loudly) plus a loaded-modules assertion after the commands run.
- [x] CHK033 Is the qualified demonstration's precondition ("empty qualified destination") paired with a stated verification or reset step before each demonstration stage (plan, sync, follow-up plan)? [Coverage, Spec §Key Entities / §R-3]
  > REMEDIATED: emptiness is verified immediately before the first plan and before the confirmed sync; the follow-up plan intentionally runs against the just-synchronized destination; stages run strictly sequentially.

## Environment & Dependency Assumptions

- [x] CHK034 Are the environment facts the demonstration depends on documented with a dated source (Infrahub 1.9.8 at `http://localhost:8000`, schema loaded on `main`, zero `InfraDevice` objects, R-3 smoke test), and is each marked as assumption rather than requirement? [Traceability, Spec §Assumptions / §R-3]
  > REMEDIATED: the environment facts now carry the preparation date (2026-07-30) in both R-3 and the Environment assumption; they remain assumption-level.
- [x] CHK035 Is the fallback when the live environment or credentials are unavailable — "record the live-environment ceiling rather than blocking" — defined concretely enough (what is recorded, where, and which DBAs it caps) to execute without interpretation? [Clarity, Spec §R-3 / §Assumptions]
  > REMEDIATED: an Assumptions informed-default defines what is recorded (affected criteria plus substitute local evidence), where (the run's delivery evidence in the feature directory), and the maximal capped set (DBA-002–005, DBA-008, DBA-009's paired comparison, DBA-011).
- [x] CHK036 Is the Prefect version pin (3.7.2, fixed for the preview) consistent with the out-of-scope item "supporting multiple Prefect major versions", and is the pin's location (optional extra constraint) specified? [Consistency, Spec §Constraints / §Out of Scope]
  > REMEDIATED: the constraint now names the pin location (the optional extra's dependency specification) and its consistency with the out-of-scope exclusion.
- [x] CHK037 Are the noted destination-schema deprecation warnings (`display_labels`, `default_filter`) recorded with a stated impact on expected outputs (none, or otherwise), so demonstration transcripts containing them are not misread as failures? [Coverage, Spec §Assumptions]
  > REMEDIATED: the Environment assumption now states the warnings have no impact on expected outputs and transcripts containing them are not failures.
- [x] CHK038 Is the supported Python version range for the optional Prefect extra stated (the base package supports 3.10–3.13 per repository docs), or is the extra's environment assumption undocumented? [Gap, Spec §Assumptions]
  > REMEDIATED: an Assumptions informed-default states the extra targets the base package's range (3.10–3.13) and the README states the supported version among its prerequisites.

## Notes

- Check items off as completed: `[x]`
- Add comments or findings inline
- Items are numbered sequentially for easy reference
- Reviewer result: 38/38 items pass; 28 items required spec remediation, applied 2026-07-30. All remediations are code- or brief-grounded readings; the two PROVISIONAL clarifications touched (fingerprint, engine defaults/lock) were sharpened in wording only. No brief defects found; no BLOCKED-ON items.

---

**Post-D005 revalidation note (2026-07-30, round-1 remediation — F4; append-only, original attestations left untouched)**: CHK026 and CHK036 attest README-prerequisite and pin-consistency facts in terms of "Prefect 3.7.2", the pre-D005 spec text. Re-checked against the D005-remediated text: the pinned version is now `prefect==3.5.0` (PROVISIONAL, CHECKPOINT, D005) with D006 companion pins inside the optional extra. CHK026's substance holds — the README's machine prerequisites remain required to be explicit (Python range, the pinned extra now at 3.5.0, reachable Infrahub) and the placeholder convention stands (tasks T033/T034); CHK036's substance holds — the 3.5.0 pin remains consistent with the out-of-scope exclusion of multiple Prefect major versions and its location (the optional extra's dependency specification) is specified in spec §Constraints and contracts/prefect-flow.md §1. Both items remain PASS against the current text; only the version literal in the original attestations is stale.
