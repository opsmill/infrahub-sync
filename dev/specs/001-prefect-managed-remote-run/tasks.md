# Tasks: Prefect-Managed Remote Infrahub Sync Run (Developer Preview)

**Input**: Design documents from `dev/specs/001-prefect-managed-remote-run/`

**Prerequisites**: `plan.md`, `spec.md` (both required), `research.md`, `data-model.md`,
`quickstart.md`, `contracts/execution-surface.md`, `contracts/prefect-flow.md`,
`contracts/run-result-and-errors.md`. Product authority: brief DB-001 v2
(`LOCAL-DP-001`), carried verbatim in spec.md (DBR-001–DBR-015, DBA-001–DBA-011).

**Tests**: REQUIRED. The spec's acceptance criteria (DBA-001–DBA-011) and Constitution
Principle V name specific automated tests and live verification evidence; test tasks
below are mandatory, not optional.

**Commit discipline (binding, plan Design Outline #4)**: commit 1 = T001 (R-1) alone;
commit 2 = T002 (R-2) alone; feature work only after both. All commits are made by the
root orchestrator; tasks below mark the required commit boundaries.

**Logging convention for new code**: new modules log via `logging.getLogger(__name__)`
under the `infrahub_sync` logger hierarchy — the repository's existing pattern
(`cli.py`, `potenda/`) and load-bearing for the DBR-012 log bridge (research
"Log bridging" decision). No `print()` in package code; no secrets in any log or
exception message (DBR-006, DBA-008).

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies on incomplete tasks)
- **[Story]**: US1–US5 from spec.md; Setup/Foundational/Polish tasks carry no story label
- Every task cites the DBR/DBA/SC/R/D identifiers it advances (Trace:) and, for
  verification tasks, the evidence it must produce (Evidence:)

---

## Phase 1: Setup (mandated enabling work — R-1, R-2, baseline)

**Purpose**: The brief's ordered pre-implementation readiness items and the inherited
baseline record. No feature work may precede T001/T002.

- [X] T001 Update the setup and workflow commands from `uv sync` to `uv sync --extra dev` in `AGENTS.md` (lines 18 and 26: the Setup block and the Required Development Workflow block) and in the verbatim mirror `.github/copilot-instructions.md` (lines 18 and 26 — see D007 below); verify with `grep -rn "^uv sync$" AGENTS.md .github/copilot-instructions.md` returning no matches; then run `uv sync --extra dev` and confirm `uv run pytest -q --collect-only` succeeds in this worktree. **Commit boundary: this change is commit 1 of the branch, alone.** Trace: R-1; spec "Mandated Enabling Work"; plan Design Outline #4; D007.
- [X] T002 Run `uv run infrahub-sync generate --name from-netbox --directory examples/` once and stage the regenerated files under `examples/netbox_to_infrahub/` (expected: four files, ~400 lines of deterministic template drift); verify by re-running the same command and confirming `git status --porcelain examples/` is empty afterward. **Commit boundary: this regeneration is commit 2 of the branch, alone (isolated baseline-hygiene commit).** **D014 note** (RATIFIED, option C, Blake Ellis, 2026-07-31): the mandated regeneration was performed and committed as commit 2; its re-run verification **cannot be relied on to pass**, because `generate`'s fixed point **moves**: three consecutive runs are byte-identical to each other across all four generated files (verified pairwise, run1 == run2 == run3), yet the committed T002 output at `e04f262` differs from today's stable output by 484 lines across 4 files (22 + 220 + 22 + 220). The emitted order is stable within a window and shifts across windows — an INHERITED generator defect (`git diff main..HEAD -- infrahub_sync/generator/` is empty). Cause of the shift is **unverified** (most plausibly a cold-vs-warm server schema cache or a schema reload; not tested, because that needs a lab-container restart, out of bounds for this run). Option B (commit the current fixed point) is **viable, not impossible**, and was **declined** when re-offered: it yields a clean tree only until the order shifts again, and a gate that passes today and fails later without anyone touching the generator is worse than one documented as failing — it hides itself. Ratified disposition remains **C**: **report** the ordering churn as inherited (see T041), with the fix deferred to the tracked issue `bug-generate-output-is-not-deterministic-across-runs.md` in the planning repo's proposed-issues directory. This does not change T002's completed status or its commit reference. Trace: R-2; D014; spec "Mandated Enabling Work"; plan Design Outline #4.
- [X] T003 Record the inherited baseline in `dev/specs/001-prefect-managed-remote-run/run-report.md` (create the file): run `uv run pytest -q` (expect 110 passed, 3 skipped), `uv run invoke lint` (expect exit 0 with pre-existing pylint `import-outside-toplevel` warnings in `infrahub_sync/potenda/__init__.py`), `uv run invoke format` (expect no diffs); note R-5 (`tests/test_potenda_parallel.py` `@pytest.mark.timeout(5)` no-op — inherited, do not fix, do not add `pytest-timeout`). **D014 correction to this task's expectations** (RATIFIED, Blake Ellis, 2026-07-31): the measured baseline is `uv run invoke lint` **exit 30, not exit 0** (29 × `C0415` + 1 × `E0213` at `infrahub_sync/__init__.py:98`, rating 9.60/10) — INHERITED, since `pylint infrahub_sync/` is pylint's only target (`tasks/linter.py:41`) and `git diff main..HEAD -- infrahub_sync/` is empty; and `uv run invoke format` must not be used (`rumdl fmt` corrupts Markdown) — use `uv run invoke linter.format`. Measured pytest is 111 passed / 2 skipped and `ty` is 3 diagnostics / exit 0. The corrected record lives in `run-report.md` §"Phase 1 — Inherited baseline"; the gate that consumes it is T039. Trace: R-4, R-5, SC-006 (baseline non-regression clause); D014. Evidence: recorded counts and command output in the run report.

**Checkpoint**: Commits 1 and 2 exist; baseline recorded; tree clean.

---

## Phase 2: Foundational (shared execution surface + fingerprint — blocks all stories)

**Purpose**: The narrow typed surface (DBR-007/008/015) and the canonical fingerprint
helper that every user story calls or verifies against. No Prefect import anywhere in
this phase (DBR-010).

- [X] T004 [P] Create `infrahub_sync/cache/fingerprint.py` with `PLAN_FINGERPRINT_FIELDS = ("action", "resource", "source_id", "attribute", "new_value")` and `compute_plan_fingerprint(run_dir: Path) -> str` implementing the binding algorithm of `contracts/run-result-and-errors.md` §3: project the five fields from `<run_dir>/plan.parquet`, serialize each row as `json.dumps(row, sort_keys=True, separators=(",", ":"))`, sort by `(resource, source_id, action, attribute)` with the full serialized row as final tie-breaker — every sort-key field normalizes `None` to `""` (`x if x is not None else ""`) so the tuple sort stays total on null-bearing rows (E12; serialization unchanged, `None` stays JSON `null`) — join with `"\n"`, UTF-8, SHA-256 hexdigest. Fully typed, concise docstring. Trace: DBA-009, SC-007; spec clarification #1 (D001); data-model §5.
- [X] T005 [P] Create `infrahub_sync/execution.py` with the type layer of `contracts/execution-surface.md`: `Operation`/`Status`/`ActionKey` literals, `PotendaFactory` alias, `RunValidationError`, `RunExecutionError`, the frozen+slots seven-field `RunResult` dataclass with `__post_init__` enforcing all data-model §2 invariants (`changed ⇔ status != "no-change" ⇔ sum(summary.values()) > 0`; `planned ⇒ operation == "plan"`; `applied ⇒ operation == "sync"`; `run_id == Path(artifact_path).name`; `set(summary) == {"create","update","delete"}`, raising `ValueError`) and wrapping `summary` in `types.MappingProxyType` via `object.__setattr__` so it cannot be mutated after validation (E14 — the field is annotated `Mapping[ActionKey, int]`), and the value-based secret-redaction helper: collect env credential values — at minimum `INFRAHUB_API_TOKEN`, plus the value of every env variable whose NAME matches `*_TOKEN`/`*_PASSWORD`/`*_SECRET`/`*_API_KEY` (E10) — plus values of `token`/`password`/`secret`/`api_key` keys in resolved adapter settings; replace occurrences with `***`; AND provide the chain-sanitization used at the `run_remote_request` wrap point (E5): redaction must cover the WHOLE cause chain (rebuild the cause as a sanitized copy, or `__suppress_context__` + redacted cause text inlined) so that a `traceback.format_exception` rendering of a wrapped error contains no unredacted original message. No `prefect` import; module stays importable in a base install. Trace: DBR-015, DBR-006; DBA-008, DBA-010; data-model §§2–3; contracts/run-result-and-errors.md §§1–2; D009.
- [X] T006 Implement `resolve_sync_instance(sync_name: str, *, directory: str) -> SyncInstance` in `infrahub_sync/execution.py` as a **tolerant per-file walk** (D010; NOT a reuse of `utils.get_all_sync`'s eager validate-everything pass): the same recursive `**/config.yml` discovery glob and exact-string `name` match as the CLI lookup, but each file read/parsed/validated individually. **Name extraction is fixed (E17), which is what makes the rules decidable**: read the file and `yaml.safe_load` it; the file's name is the top-level `name` key of the loaded mapping (`data.get("name")`), and is UNDETERMINABLE when the read raises `OSError`, the parse raises `yaml.YAMLError`, or the loaded object is not a mapping. Exactly one of three disjoint rules applies per file: (a) name determinable AND equal to the request → validate the parsed data as `SyncConfig` and return the `SyncInstance` constructed with `directory=str(<the matched config.yml>.parent)` (binding — V3; matching `utils.get_all_sync`, `utils.py:129`) — the matched file's own parent, NOT the configured root passed in as `directory`, because `utils.import_adapter` resolves the generated adapter at `<sync_instance.directory>/<adapter.name>/sync_adapter.py` (`utils.py:74-77`) and the qualified demonstration depends on `examples/custom_adapter/mockdb/sync_adapter.py`; passing the root would make that path miss for any nested configuration and silently fall through to the plugin loader; on validation failure → `RunValidationError` naming the logical name and the file path ONLY, with the parse detail never chained verbatim (pydantic's `input_value` echo can leak file contents the redactor never collected); (b) name determinable AND different (including a mapping with no/non-string `name`, which can never equal the request) → skipped silently, DEBUG at most; (c) name UNDETERMINABLE → skipped with a WARNING naming the file path ONLY (never contents, never the exception's rendered detail), counted, and resolution CONTINUES — one broken neighbor never blocks other names, and a bad-YAML file can never be "the matched one". No match at all → `RunValidationError` naming the logical name, and when N > 0 files were skipped as UNDETERMINABLE the message additionally states that N file(s) could not be read (COUNT only — no names, no contents) so a typo is distinguishable from a broken config. The requested value is never used to construct a filesystem path; never echo directory contents, file contents, or credential values. The CLI keeps calling `utils.get_instance` unchanged. Depends on T005 (same file). Trace: DBR-005; DBA-007; SC-004; data-model §1 validation steps 3–4; D010; E17.
- [X] T007 Implement `execute_run(sync_instance, *, operation, confirm_writes=False, branch=None, show_progress=None, verbosity=logging.INFO, run_id=None, concurrent_load=True, full_extract=True, allow_rowcount_drop=False, continue_on_error=False, print_diff=True, potenda_factory=None, _lock_timeout=60.0, _serial_load_error=None, _lock_already_held=False) -> RunResult` in `infrahub_sync/execution.py` per `contracts/execution-surface.md` steps 1–7 with **D009 failure semantics — execute_run raises ORIGINAL exception types and never wraps into `RunExecutionError`**: (1) validate `operation` membership and the `confirm_writes` gate (`operation="sync"` unconfirmed → `RunValidationError` stating `confirm_writes=true` is required to run `operation=sync`) BEFORE any adapter construction — the only surface-typed raise in this function; (2) acquire `infrahub_sync.cache.locks.pipeline_lock(sync_instance.name, timeout=_lock_timeout)` (default 60 s as today; `_lock_timeout` is the sanctioned test seam — E15); `filelock.Timeout` propagates UNCHANGED (today's CLI traceback); when `_lock_already_held=True` (CLI-only seam, set ONLY by `sync_cmd`'s serial branch per T026 — `run_remote_request` never sets it) SKIP the acquisition and run steps 3–7 in the caller's already-held lock scope, since a second same-process `FileLock` on the same path blocks the full timeout and then raises `filelock.Timeout` (probed — E21); (3) build the engine via `potenda_factory` (default `utils.get_potenda_from_instance`) with NO surrounding catch — factory `ValueError`/`ImportError` propagate with their original types (the CLI's wrapper factory owns the prefixed abort, T025) — using the PINNED call shape (E23): all seven kwargs always passed explicitly (`sync_instance`, `branch`, `show_progress`, `verbosity`, `run_id`, `continue_on_error`, `concurrent_load`) for both operations, since today's two commands pass different subsets whose omitted values equal these defaults (behaviorally identical for `utils.get_potenda_from_instance`, but visible to a fake factory); then, before any RunFile exists, set `ptd.force_full_extract = full_extract` and carry today's guard verbatim — `if ptd.run_dir is None: raise RuntimeError("get_potenda_from_instance did not allocate a run_dir")` (`cli.py:143-145`/`241-243`, E22 — required for CLI parity AND to narrow `Potenda.run_dir: Path | None` for T039's ty gate); (4) `plan` = today's `cli.py` diff lifecycle exactly (RunFile `mode="diff"` `running→dry-run`, `force_full_extract`, `load_both_sides`, `diff`, `write_plan`, diff-string log, `summary={"resources": len(top_level)}`, `finished_at`, "Cached run %s at %s"); (5) `sync` = today's serial branch exactly (RunFile `mode="sync"`, `load_both_sides` with today's inner narrow catch preserved: `except ValueError` → mark run.json `failed`, save, then invoke `_serial_load_error(exc)` when the CLI provided it — unprefixed abort at the site, `cli.py:263-268` — else re-raise the original unchanged; then `check_rowcount_guardrail`, `diff`, `write_plan`, optional diff print, `sync(diff)` + timing log only when diffs exist else "No difference found. Nothing to sync", `persist_baseline_counts`, `running→applied`, `summary={"resources": ..., "mode": "serial"}`, "Sync run %s at %s"); (6) any other failure inside 4/5 uses the PRESERVED `9edc1bc` pattern verbatim: `except Exception: run_file.status = "failed"; run_file.save(); raise` — bare re-raise of the original, documented at the site as the preserved pattern by a comment and **without any `# noqa: BLE001`** — the mechanism-conditioned E16 rule (contracts/execution-surface.md "BLE001 suppression rule"): the directive is added if and only if ruff reports BLE001 for the code as actually written (verify with `uv run ruff check`, never speculatively), and at THIS site it does not, because BLE001 does not fire on a blind `except` whose handler re-raises the caught exception (probed: `except Exception: <mark failed>; raise` is clean; adding the directive makes ruff report `RUF100 Unused noqa directive` and exit 1, failing T039's gate) — note the sibling site in T008 resolves the OTHER way (D009); (7) derive `RunResult` with single-source derivation: `summary`/`changed`/`status` ALL counted from the in-memory row list (`ptd._diff_to_rows(diff)` or an equivalent shared function), the same rows passed to `write_plan`, NEVER by re-reading `plan.parquet` (E8; nested-child caveat per contract step 7). No sanitize-and-wrap in this function. Depends on T006. Trace: DBR-004, DBR-007, DBR-009, DBR-015; DBA-006, DBA-010; Constitution I/II; spec clarification #3 (D003); D009.
- [X] T008 Implement `run_remote_request(sync_name, operation="plan", confirm_writes=False, branch=None, *, config_directory, _potenda_factory=None, _lock_timeout=60.0) -> RunResult` in `infrahub_sync/execution.py`: `resolve_sync_instance` then `execute_run` with every engine option at its `9edc1bc` CLI default EXCEPT `show_progress=False`; the two private seams mirror `execute_run`'s and are forwarded verbatim (`potenda_factory=_potenda_factory`, `_lock_timeout=_lock_timeout`), defaulting to the production values — they are NOT part of the remote contract, are never set by the Prefect flow (which passes request parameters plus the config directory only), and exist solely as sanctioned test seams so tests can drive the REAL wrap boundary without improvising a monkeypatch target (E18/X17 — used by T011/T022/T023/T029); `_serial_load_error` is never set (CLI-only). No public parameter accepts paths, CLI fragments, credentials, or environment overrides. This function is **THE sanitize-and-wrap boundary (D009)**: `RunValidationError` passes through; `filelock.Timeout` → `RunExecutionError` naming the sync and timeout; factory `ValueError` → `RunExecutionError` preserving today's "Failed to initialize the Sync Instance: ..." wording; `ImportError` → `RunExecutionError` naming the adapter import failure; every other escaping exception → sanitized `RunExecutionError` naming stage and cause — all via the T005 chain-sanitizing redaction (whole cause chain, E5); the broad catch is a boundary translation (always re-raised typed), documented by a comment at the site and **WITH a targeted `# noqa: BLE001` on the `except Exception` line** — the mechanism-conditioned E16 rule (contracts/execution-surface.md "BLE001 suppression rule"): the directive is added if and only if ruff reports BLE001 for the code as actually written (verify with `uv run ruff check`, never speculatively), and at THIS site it does, because every chain mechanism E5 permits fires it. Probed with `uv run ruff check --no-cache --select BLE`: `except Exception as exc: raise E(msg) from exc` is clean, but `... from None` FIRES and `... from RuntimeError(str(exc))` FIRES — and those two are exactly what `contracts/run-result-and-errors.md` §2 (E5, binding) allows here (rebuilt sanitized cause, or `__suppress_context__` with the redacted text inlined), while the one clean form (plain `from exc`) is the unredacted-cause traceback leak E5 forbids; the comment at the site names E5 as the reason the clean form is unavailable. The step-6 site in T007 resolves the OTHER way (no directive there). Depends on T007. Trace: DBR-003, DBR-005, DBR-006, DBR-015; data-model §1 pinned engine options; spec clarification #3 (D003); D009.
- [X] T009 [P] Create `tests/test_plan_fingerprint.py`: unit tests for `compute_plan_fingerprint` — determinism across two identically-planned run dirs, row-order independence, exclusion of timestamps/run-ids/paths (two runs differing only in run_id/dir hash equal), tie-breaker behavior on synthetic duplicate keys INCLUDING a null-bearing tie-breaker case (rows tying on the leading fields with `None` in a sort-key field must sort without `TypeError` and hash deterministically — E12), and a fixed-vector digest test; tests may not reimplement the algorithm (assert against the helper, not a copy). Depends on T004. Trace: DBA-009, SC-007; contracts/run-result-and-errors.md §§3–4; D001.
- [X] T010 Create `tests/test_execution_surface.py` (part 1 — result and redaction): assert `RunResult` exact field set (`len(dataclasses.fields()) == 7`, `slots` rejects extra attributes), immutability (`FrozenInstanceError` on field assignment AND `TypeError` on `result.summary["create"] += 1` — the mappingproxy half, E14), every data-model §2 invariant (positive and violating cases → `ValueError`), and redaction as raised by the remote composition (D009 — the typed errors originate in `resolve_sync_instance`/step-1 validation/`run_remote_request`, never in `execute_run`'s lifecycle handling): seeded secret values in env — including values reached only via the `*_TOKEN`/`*_PASSWORD`/`*_SECRET`/`*_API_KEY` name patterns, E10 — and in adapter settings are replaced by `***` in `RunValidationError`/`RunExecutionError` messages, and a `traceback.format_exception` rendering of a wrapped error contains no unredacted original message anywhere in the cause chain (E5). Depends on T005. Trace: DBA-010, SC-008; DBR-015; contracts/run-result-and-errors.md §§1–2; D009.
- [X] T011 Extend `tests/test_execution_surface.py` (part 2 — refusals and lifecycles): parametrized SC-004 negative set for `sync_name` (`"nope"` unknown, `"../custom-example"`, an absolute path, `"a/b"` separator, `"--help"` flag-like, `"$(touch ...)"` shell-metacharacter/command-substitution) each refused as `RunValidationError` with filesystem spies proving no read outside the configured directory and subprocess spies (patched `subprocess.Popen`/`os.system`) proving no subprocess start; matched-but-invalid negative (E17 rule (a) — the file's `name` IS determinable): a `config.yml` whose parsed top-level `name` equals the request but whose data fails `SyncConfig` validation → `RunValidationError` naming the logical name and the file path ONLY, never including file contents and never chaining the parse detail verbatim (D010); bad-YAML case (E17 rule (c) — a file that fails to parse has an UNDETERMINABLE name and therefore can NEVER be "the matched one"): an unparseable `config.yml` is skipped with a WARNING naming its path only (no contents, no rendered parse detail) and resolution continues, so when nothing else matches the request the terminal error is the ordinary unknown-name `RunValidationError` naming the logical name AND stating that 1 file could not be read (count only) — assert the WARNING, the count mention, and that neither the file contents nor the YAML error detail appears anywhere; tolerant-walk positive (D010): an UNRELATED invalid/unparseable `config.yml` present in the directory → the requested valid name still resolves, with a WARNING logged naming only the offending file; unconfirmed `operation="sync"` refused BEFORE adapter construction (spy `potenda_factory` never called) with the required message; pipeline-lock contention via the sanctioned `_lock_timeout` seam (held lock, shortened timeout — E15): `execute_run` lets `filelock.Timeout` propagate unchanged (D009), and the same scenario through `run_remote_request` — driven through THAT function's own `_lock_timeout` private seam (E18/X17), never an ad-hoc patch of `pipeline_lock` and never the 60 s default — → `RunExecutionError` naming sync and timeout, bounded not hanging; one execution-failure case: the factory raising `ValueError` for a missing credential (e.g. unset `INFRAHUB_API_TOKEN`) propagates as `ValueError` from `execute_run` and arrives via `run_remote_request` — the fake factory injected through THAT function's `_potenda_factory` private seam (E18) — as a sanitized `RunExecutionError` whose message names the missing input; for the infrahub-adapter case the assertion sits on `run_remote_request`'s WRAPPER message, which names the `INFRAHUB_ADDRESS`/`INFRAHUB_API_TOKEN` environment variables by NAME and never any value (D012 option A: `infrahub_sync/adapters/infrahub.py` is NOT modified by this delivery, so no CLI-visible message changes — assert nothing about the adapter's own wording); a `branch` passthrough assertion: an explicit `branch` value passed to `execute_run`/`run_remote_request` reaches the spy `potenda_factory` unchanged (via `potenda_factory` / `_potenda_factory` respectively), and omitting it yields the CLI default (`None`); successful `plan` via a fake `potenda_factory` fixture → `status in {"planned","no-change"}`, `run.json` `mode="diff"`/`dry-run`, `plan.parquet` present, `RunResult` fields correct. Depends on T007, T008, T010 (same file). Trace: DBA-006, DBA-007, DBA-010; SC-004, SC-008; DBR-003 (branch passthrough), DBR-004, DBR-005, DBR-008; D009, D010.

**Checkpoint**: `infrahub_sync/execution.py` and `infrahub_sync/cache/fingerprint.py`
complete and unit-tested; `uv run pytest -q tests/test_execution_surface.py
tests/test_plan_fingerprint.py` green; no `prefect` import anywhere in the package.

---

## Phase 3: User Story 1 — Remote plan through Prefect (Priority: P1) 🎯 MVP

**Goal**: A developer installs the optional extra, starts a default self-hosted Prefect
Server, serves the packaged flow, and a remote caller runs and observes a real read-only
`plan` through Prefect's REST API and UI.

**Independent Test**: Against an empty qualified destination, submit `operation=plan`
for `custom-example` via `POST /api/deployments/{id}/create_flow_run`; a flow-run ID is
returned synchronously, the run reaches COMPLETED, the plan shows five creates, and the
lifecycle plus bridged `infrahub_sync` log lines are visible in Prefect (quickstart
Scenario 1; DBA-002/003/004).

- [X] T012 [P] [US1] Change the dependency declarations in `pyproject.toml` per **D005 option D** (RATIFIED, Blake Ellis, 2026-07-30) — this is a base-dependency change, not an extra-only addition. In base `dependencies`: replace `"diffsync[redis]>=2.1,<3.0"` with the two entries `"diffsync>=2.1,<3.0"` and `"redis>=4.3,<9"`. Add `[project.optional-dependencies]` entry `prefect = ["prefect==3.8.1"]` — **exactly that one pin, no companion pins** (D006 withdrawn: its `importlib-metadata>=4.4` and `fastapi>=0.111,<0.121` pins repaired prefect-3.5.0 defects that are verified fixed at 3.8.1 — the stdlib `importlib_metadata` alias now lives in `prefect/utilities/compat.py`, and the deployment-name route returns HTTP 200 under fastapi 0.141.1). Comment the two base entries with WHY, because the reason is not local: `diffsync[redis]`'s extra caps `redis>=4.3,<5.0` while `prefect>=3.6` → `pydocket>=0.19.0` → `redis>=5`, so the extra cap must go; `redis` is declared DIRECTLY because `infrahub_sync/utils.py:11` imports `diffsync.store.redis.RedisStore` unconditionally (dropping `[redis]` without a direct declaration would break `import infrahub_sync.utils` for every user); and the floor is deliberately permissive (`>=4.3`, NOT `>=5`) so a downstream consumer that still requires `diffsync[redis]` keeps resolving — verified: with `redis>=4.3,<9` that combination resolves at redis 4.6.0, with `redis>=5` it is unsatisfiable. diffsync's `<5.0` cap is stale rather than real (its redis store verified functionally intact on redis-py 4.6.0/5.0/6.4/7.0/8.1.0 — 26/26 checks incl. `diff_from`/`diff_to`/`sync_from` between two Redis-backed adapters; 4.6.0 control and 8.1.0 byte-identical) and the override is permanent (no diffsync 3.x, no open PR raising the cap). Explicitly run `uv lock` to regenerate the committed lockfile and stage `uv.lock` alongside `pyproject.toml` in the SAME commit (E9 — otherwise T041's clean-tree gate fails on the stray lock diff). Verification, all three required: (a) `uv sync --extra dev --extra prefect` resolves and `uv run python -c "import prefect; print(prefect.__version__)"` prints `3.8.1` — gate-verified resolution of the patched set for cross-checking: prefect 3.8.1, redis 8.1.0, fastapi 0.141.1, starlette 1.3.1, pydantic 2.13.4, diffsync 2.2.3, infrahub-sdk 1.22.2, pyarrow 21.0.0, typer 0.27.0, uvicorn 0.52.0, griffe 2.1.0, pydocket 0.23.1; (b) a base-only install still works and imports WITHOUT prefect — `uv pip install .` into a clean venv, then `python -c "import infrahub_sync.utils, redis, diffsync.store.redis"` succeeds and `prefect` is not importable (the redis client must be present from the direct declaration alone); (c) `git status --porcelain` shows only `pyproject.toml` and `uv.lock` from this task. Depends on nothing; T012a verifies the redis-store code path. Trace: D005 (option D); research F1 + "Gate-ratified resolution", probes a₁–a₆; DBR-010 (optionality), DBR-014 (no backlog item, engine untouched); contracts/prefect-flow.md §1.
- [X] T012a [P] [US1] Create `tests/test_redis_store_compat.py` — a real check of the redis-store code path under the upgraded client, because the repository's own suite has **zero** redis coverage today (`grep -rl redis tests/` returns nothing), so the base-dependency change in T012 would otherwise ship unexercised. No prefect dependency and no `importorskip` — this test must run and pass in a base install (redis is now a base dependency). Server-free assertions: (1) `from diffsync.store.redis import RedisStore` imports cleanly under the installed client (this is the compatibility guarantee that matters for every user, since `utils.py:11` imports it unconditionally); (2) the redis-py API surface the store depends on still exists — `redis.Redis`, `redis.Redis.from_url`, and instance attributes `ping`, `get`, `set`, `exists`, `delete`, `scan_iter`; (3) the store↔client error contract still holds: `RedisStore(host="127.0.0.1", port=<closed port>)` raises `diffsync.exceptions.ObjectStoreException` (the store catches `redis.exceptions.ConnectionError`, so this fails loudly if the client's exception hierarchy moves — see `diffsync/store/redis.py`); (4) `RedisStore(url=..., host=...)` raises `ValueError` (the store's own guard, reachable without a server). Plus ONE opt-in functional round-trip marked `@pytest.mark.integration` and skipped unless a `REDIS_URL`-style env var points at a reachable server: build two `RedisStore`-backed adapters and assert an `add`/`get`/`get_all`/`remove` round-trip plus `diff_from`/`sync_from` between them — mirroring the gate's own verification. If no redis server is available at delivery time, record that explicitly in the run report as a live-environment ceiling entry alongside the gate's own result (26/26 checks on redis-py 4.6.0/5.0/6.4/7.0/8.1.0 against a redis 8.4.0 server) — the recorded fact substitutes for the local run, never a prose assertion that it "should work". Depends on T012. Trace: D005 (option D — base `redis` declaration); DBR-014 (no behavior change to the engine); contracts/prefect-flow.md §1 (`RedisStore` opt-in note: `utils.py:195-205` defaults to `LocalStore()`, so this is import/API compatibility, not a feature).
- [X] T013 [P] [US1] Create `infrahub_sync/orchestration/__init__.py` — empty module docstring only; no `prefect` import at package level (the orchestration package is the ONLY place allowed to import prefect, and only in its leaf modules). Trace: DBR-010; DBA-001; plan Project Structure.
- [X] T014 [US1] Create `infrahub_sync/orchestration/flow.py` per `contracts/prefect-flow.md` §§2+4: module-level comment explaining the deliberate ABSENCE of `from __future__ import annotations` (research F3 — deferred annotations break prefect's run-time parameter validation with `PydanticUndefinedAnnotation`; observed on 3.5.0, mechanism version-generic, so the omission is kept at 3.8.1 and T016's parameter-contract assertions confirm it) and a module docstring noting the optional `prefect` extra requirement for programmatic importers (X4); constants `FLOW_NAME = "infrahub-sync"`, `DEPLOYMENT_NAME = "run"` (X12 — lookup path `/deployments/name/infrahub-sync/run`, leaving `.../infrahub-sync/<verb>` room for future sibling deployments; rationale recorded in the contract), `CONFIG_DIR_ENV = "INFRAHUB_SYNC_CONFIG_DIRECTORY"`; `RunLoggerBridge(logging.Handler)` forwarding `infrahub_sync`-hierarchy records to the Prefect run logger preserving level and origin logger name; `@flow(name=FLOW_NAME) def infrahub_sync_run(sync_name: str, operation: Literal["plan","sync"] = "plan", confirm_writes: bool = False, branch: str | None = None) -> dict` whose body (in order) gets the run logger, attaches the bridge to `logging.getLogger("infrahub_sync")` AND owns that logger's LEVEL (E4: capture `logging.getLogger("infrahub_sync").level`, set `logging.INFO` before the surface call — a handler alone never defeats `isEnabledFor`) inside `try/finally` (handler removal AND captured-level restoration in the same `finally`), reads `os.environ[CONFIG_DIR_ENV]` (missing → `RunExecutionError` defensively), calls `run_remote_request(...)` in-process (never a subprocess), logs one summary line in the FIXED key=value format (`"run %s finished: status=%s changed=%s summary=create:%d,update:%d,delete:%d artifact=%s"` — the supported remote observation surface, X7; never a dict repr), and returns an **asdict-SHAPED dict built by explicit seven-key construction** — `out = {f.name: getattr(result, f.name) for f in dataclasses.fields(result)}` then `out["summary"] = dict(result.summary)`, with an inline comment recording WHY: `dataclasses.asdict(result)` deep-copies field values and the E14 `summary` mappingproxy is not deep-copyable, so it raises `TypeError: cannot pickle 'mappingproxy' object` (root-probed) and would fail every successful run at return time (X15) — do not simplify it back to `asdict`; exceptions propagate so Prefect records FAILED with the sanitized message. Exactly these four parameters — no path/credential/env-override parameter exists. Depends on T008, T012, T013. Trace: DBR-001, DBR-003, DBR-005, DBR-006, DBR-008, DBR-011, DBR-012; DBA-003, DBA-004; SC-001, SC-002; spec clarification #4 (D004); research probes b, c₁, c₂, d₁.
- [X] T015 [US1] Create `infrahub_sync/orchestration/serve.py` runnable as `python -m infrahub_sync.orchestration.serve` per `contracts/prefect-flow.md` §3: at startup FIRST guard the prefect import — an `ImportError` importing prefect (via `orchestration.flow`) exits non-zero with exactly one line naming the optional extra and the install command (`prefect is not installed - install the optional integration: pip install -e '.[prefect]'`), never a bare traceback (X4); then read `INFRAHUB_SYNC_CONFIG_DIRECTORY`; if unset, empty, or not an existing directory, emit one error line naming the variable and exit non-zero BEFORE any deployment is served; error lines use `logging` (lastResort handler) or `sys.stderr.write` — NEVER `print()`, which the repo's AST no-print test rejects (E13); otherwise call `infrahub_sync_run.serve(name=DEPLOYMENT_NAME)` (locally served deployment; no work pool, no worker; default `enforce_parameter_schema=True`); configurations under the directory are re-resolved per run (no restart needed). Depends on T014. Trace: DBR-002; DBA-002; spec clarification #2 (D002); data-model §4.
- [X] T016 [US1] Create `tests/orchestration/__init__.py` and `tests/orchestration/test_flow.py` with `pytest.importorskip("prefect")` module guard: assert the flow's parameter contract (exactly `sync_name`, `operation` default `"plan"`, `confirm_writes` default `False`, `branch` default `None` — no fifth parameter); assert `RunLoggerBridge` forwards an `infrahub_sync.potenda` child-logger record to a stub run logger preserving level and logger name and is removed — with the captured `infrahub_sync` logger level restored — after the body (including on exception); assert bridge completeness: emit a known count of records across the `infrahub_sync` hierarchy at or above the effective level and assert the stub run logger received exactly that count — records emitted at effective level == records forwarded (the SC-002 denominator); assert level ownership (E4): force root/Prefect logging to WARNING (e.g. `logging.getLogger().setLevel(logging.WARNING)` with the `infrahub_sync` logger left `NOTSET`) and assert bridged INFO records STILL arrive — the flow's level management, not ambient configuration, makes INFO effective; assert a successful in-process flow call (patched `run_remote_request`) returns the asdict-SHAPED seven-key dict with `summary` as a plain `dict` (X15 — the assertion is unchanged; only the flow's construction mechanism is); assert serve-start validation by invoking the `serve.py` validation function with the env var unset/pointing at a file → non-zero exit/error naming `INFRAHUB_SYNC_CONFIG_DIRECTORY`. Isolation (X10): any test that actually EXECUTES the flow uses the named session fixture — `prefect.testing.utilities.prefect_test_harness()` (or `PREFECT_HOME`/`PREFECT_API_URL` pointed at `tmp_path`) — so unit runs never touch the developer's real `~/.prefect`; pure contract assertions (parameter signature, bridge behavior) stay harness-free for speed. Depends on T014, T015. Trace: DBR-003, DBR-012; DBA-002 (serve validation), DBA-004; SC-002 (completeness denominator); spec clarifications #2/#4 (D002/D004).
- [X] T017 [US1] Create `tests/integration/test_remote_run_live.py` marked `@pytest.mark.integration` (opt-in per `pyproject.toml` marker; requires `INFRAHUB_ADDRESS` + `INFRAHUB_API_TOKEN` in the environment and a live Prefect server + served deployment): drive quickstart Scenario 1 programmatically — resolve the deployment id via `GET /api/deployments/name/infrahub-sync/run`, `POST .../create_flow_run` with `{"parameters": {"sync_name": "custom-example", "operation": "plan"}}`, assert the id is returned synchronously, poll to COMPLETED, assert `POST /api/logs/filter` contains bridged `infrahub_sync` lifecycle lines and the fixed-format summary line reporting `summary=create:5,update:0,delete:0`. Skip guards in the existing house style (X8) — `pytest.skip` with a reason naming what is missing when: `PREFECT_API_URL` is unset; the Prefect API is unreachable; or the deployment lookup 404s ("serve not running — start `python -m infrahub_sync.orchestration.serve`") — so a developer with only Infrahub up gets a clean skip, not failures or a dead-API poll loop. Also update the `integration` marker help text in `pyproject.toml` to mention the Prefect-side prerequisites (live Prefect server, `PREFECT_API_URL`, a separately started served deployment). Depends on T014, T015. Trace: DBA-003, DBA-004; SC-001, SC-002; DBR-011.
- [X] T018 [US1] LIVE verification (DBA-002): in an environment with the extra installed, start `uv run prefect server start` (default local database + built-in UI) and, from the REPOSITORY ROOT (X1/E11 — the qualified example's `./` paths and the cache root resolve against the serve process's CWD), with `PREFECT_API_URL`, `INFRAHUB_SYNC_CONFIG_DIRECTORY="$PWD/examples/custom_adapter"` (scoped per X6), `INFRAHUB_ADDRESS`, `INFRAHUB_API_TOKEN` set, start `uv run python -m infrahub_sync.orchestration.serve`; also demonstrate the serve-start failure by unsetting the config-directory variable once. Evidence (append to `dev/specs/001-prefect-managed-remote-run/run-report.md`): reproduction transcript of the documented commands, `GET $PREFECT_API_URL/deployments/name/infrahub-sync/run` showing `status: "READY"` and `enforce_parameter_schema: true`, the failed-start line naming `INFRAHUB_SYNC_CONFIG_DIRECTORY`, and confirmation no external database or worker service was started. If the live environment is unavailable, record the live-environment ceiling per R-3 instead of blocking. Trace: DBA-002; DBR-001, DBR-002; spec clarification #2 (D002).
- [X] T019 [US1] LIVE verification (DBA-003 + DBA-004): verify the lab destination holds zero `InfraDevice` objects (reset a disposable one if not); run quickstart Scenario 1's REST calls; confirm the flow-run ID arrives in the synchronous create response, state reaches COMPLETED, the plan reports exactly five creates / zero updates / zero deletes, the runner-local run directory `.infrahub-sync-cache/custom-example/<run_id>/` contains `run.json` (`status: dry-run`, `mode: diff`) and `plan.parquet` with five creates, and the run lifecycle plus bridged `infrahub_sync` log lines are visible via `POST /api/logs/filter` and in the UI at `http://127.0.0.1:4200`. Evidence (run report): API request/response transcripts, flow-run record, `run.json` contents, plan summary, log excerpt, UI screenshot or state listing. Live-environment ceiling fallback per R-3. Depends on T018. Trace: DBA-003, DBA-004; SC-001, SC-002; DBR-001, DBR-003, DBR-011, DBR-012; R-3.

**Checkpoint**: US1 fully demonstrable — remote plan runs and is observable. MVP.

---

## Phase 4: User Story 2 — Explicitly confirmed destination write (Priority: P2)

**Goal**: A remote caller who reviewed the plan applies it with `operation=sync` +
`confirm_writes=true` through the same surface the CLI uses; the change is observable
at the destination and a follow-up plan converges to no-change.

**Independent Test**: Against a reset empty qualified destination, submit
`operation=sync` with `confirm_writes=true`; the run completes, five `InfraDevice`
objects exist, and a follow-up plan reports no changes (quickstart Scenario 2; DBA-005).

- [X] T020 [US2] Extend `tests/test_execution_surface.py` with the confirmed-sync lifecycle: successful `operation="sync"`+`confirm_writes=True` via the fake `potenda_factory` → `RunResult(status="applied")` with correct summary, `run.json` `mode="sync"`/`status="applied"`/`summary.mode="serial"`, `persist_baseline_counts` called, timing log only when diffs exist; a second run against the already-synchronized fake state → `status="no-change"`, `changed=False`, all-zero summary, "No difference found. Nothing to sync" logged (idempotent reconciliation). Depends on T007. Trace: DBA-005 (automated analog), DBA-010 (sync-side result schema); SC-003, SC-008; DBR-003, DBR-004; Constitution II.
- [X] T021 [US2] LIVE verification (DBA-005): verify/reset the destination to zero `InfraDevice` objects; strictly sequentially (each run terminal before the next): (1) `POST .../create_flow_run` with `{"parameters": {"sync_name": "custom-example", "operation": "sync", "confirm_writes": true}}` → COMPLETED; (2) observe exactly five `InfraDevice` objects (`core01..core03`, `edge01`, `edge02`) at the destination via the Infrahub API; (3) follow-up `operation=plan` run → COMPLETED with `status: no-change`, `changed: false`, all-zero summary. Evidence (run report): all three API transcripts, the destination object listing, and the no-change result. No concurrent destination-writing runs (spec edge case 5). Live-environment ceiling fallback per R-3. Depends on T014, T015, T019. Trace: DBA-005; SC-003; DBR-003, DBR-004; Constitution I/II.

**Checkpoint**: Full vertical slice (plan → confirmed apply → converged no-change plan)
demonstrated.

---

## Phase 5: User Story 3 — Safety refusals and secret hygiene (Priority: P3)

**Goal**: Unconfirmed writes and unresolvable/path-like/command-like configuration names
fail clearly before any adapter loads; no credential value ever reaches Prefect-visible
state.

**Independent Test**: `operation=sync` without confirmation fails before either adapter
loads with the destination unchanged; unknown/path-like/command-like `sync_name` values
are refused with no out-of-directory read and no subprocess; seeded canaries appear
nowhere Prefect-visible (quickstart Scenario 3; DBA-006/007/008/010).

- [X] T022 [US3] Add the canary-redaction scan to `tests/orchestration/test_flow.py`: seed obviously-unique canary values as `INFRAHUB_API_TOKEN` (env), as a pattern-matched env credential (e.g. `NETBOX_TOKEN` — the E10 name-pattern half), and as `token`/`password` values in a temp configuration's adapter settings; patch point: a fake engine injected through `run_remote_request`'s `_potenda_factory` private seam (E18 — the sanctioned mechanism; do NOT patch `run_remote_request` itself and do NOT improvise a module-global monkeypatch) plus the temp configuration directory ONLY — the real surface (`run_remote_request` → `execute_run`) and the real redaction path stay under test; because the flow itself never sets the seams, the halves that must execute `infrahub_sync_run` pre-bind the seam at the flow module's `run_remote_request` reference (e.g. `functools.partial(run_remote_request, _potenda_factory=fake)`) so the REAL composition and redaction still run — never substitute a stub for `run_remote_request`; execute one successful flow run and one failing run through that surface (flow-executing runs use the X10 isolation fixture — `prefect_test_harness()` or `PREFECT_HOME`/`PREFECT_API_URL` at `tmp_path`); the failing run's induced fault embeds a canary in the ORIGINAL exception message so the whole-cause-chain redaction is exercised (E5); assert the canary strings appear NOWHERE in flow parameters, the returned result dict, captured/forwarded log records (bridge output), raised exception messages/state messages, or a full `traceback.format_exception` rendering of the raised error (every message in the `__cause__`/`__context__` chain — E5). Depends on T014, T016. Trace: DBA-008; SC-005; DBR-006, DBR-012; contracts/run-result-and-errors.md §2; D009.
- [X] T023 [US3] Add flow-level failure tests to `tests/orchestration/test_flow.py`: in-process flow call with `operation="sync"` unconfirmed → raises `RunValidationError` (Prefect would record FAILED) with the confirm-writes message and a spy `potenda_factory`/adapter never constructed; unknown `sync_name` → `RunValidationError` naming the logical name; one execution fault (unreachable system via a fake engine injected through `run_remote_request`'s `_potenda_factory` private seam — E18, the sanctioned mechanism; pre-bound at the flow module's `run_remote_request` reference for the flow-executing half, per T022, since the flow never sets seams itself) → `RunExecutionError` wrapped at the `run_remote_request` boundary (D009), sanitized, and no successful result/dict produced. Flow-executing tests use the X10 isolation fixture (`prefect_test_harness()` or `PREFECT_HOME`/`PREFECT_API_URL` at `tmp_path`); contract-only assertions stay harness-free. Depends on T014, T016. Trace: DBA-006, DBA-010 (flow-level halves); SC-004, SC-008; DBR-004, DBR-005, DBR-015; D009.
- [X] T024 [US3] LIVE verification (DBA-006 + DBA-007 remote form, spec edge case 1): via REST against the served deployment — (a) unconfirmed sync → run FAILED with state message explaining `confirm_writes=true` is required; destination verified unchanged (still the pre-run `InfraDevice` count); (b) `"operation": "apply"` → HTTP 409 with the `'apply' is not one of ['plan', 'sync']` detail and NO flow run created (run inventory unchanged), no `RunResult`, no log lines, no new run directory; (c) each SC-004 negative `sync_name` (`nope`, `../custom-example`, `/etc/passwd`, `a/b`, `--help`, `$(touch /tmp/pwned)`) → run FAILED naming the logical name; `/tmp/pwned` absent afterward. Evidence (run report): request/response transcripts, destination count before/after, run-inventory counts, `ls /tmp/pwned` failure, plus a one-line canary/secret grep over all collected REST transcripts (flow-run records, state messages, log payloads) proving no credential value appears — the DBA-008 "Prefect run metadata" half, live form. Live-environment ceiling fallback per R-3. Depends on T018. Trace: DBA-006, DBA-007, DBA-008 (live transcript grep); SC-004; DBR-004, DBR-005, DBR-008; research probe d₁.

**Checkpoint**: All safety refusals verified at unit, flow, and live levels.

---

## Phase 6: User Story 4 — Base stays Prefect-free; CLI parity (Priority: P4)

**Goal**: The CLI `diff` lifecycle and serial `sync --no-parallel` branch run through the
shared surface with zero user-visible change; base install/import/CLI never touch
Prefect; CLI `diff` and remote `plan` agree exactly.

**Independent Test**: Clean venv without the extra imports the package and runs CLI
sanity with Prefect absent; DBA-009 test population passes unmodified; CLI `diff` vs
remote `plan` on reset fixtures agree on status/changed/summary/fingerprint (quickstart
Scenarios 0 and 4; DBA-001/009).

- [X] T025 [US4] Refactor `diff_cmd` in `infrahub_sync/cli.py` to delegate to `execution.execute_run` per D009 (contracts/execution-surface.md "Failure semantics" — identity by construction, no `__cause__` mapping): keep the existing `get_instance` resolution (name OR config_file — CLI-only flexibility) and `adapter_path` merging in the command body, and **DELETE the command body's own `with pipeline_lock(sync_instance.name):` wrapper** (`cli.py:129`) — `execute_run` step 2 now owns the lock, and keeping both self-deadlocks: probed, a second `FileLock` on the same path in the SAME process times out, so every `diff` would block for the full 60 s timeout and then raise `filelock.Timeout` (E21); drop the now-unused `pipeline_lock` import from `cli.py` if nothing else in the module uses it; call `execute_run(sync_instance, operation="plan", confirm_writes=False, branch=..., show_progress=..., verbosity=..., run_id=..., concurrent_load=..., full_extract=..., potenda_factory=<CLI wrapper factory>)` where the wrapper factory keeps today's narrow handler at the construction site in `cli.py` — `try: return get_potenda_from_instance(**kwargs)` (module global resolved at call time, keeping existing patches on `infrahub_sync.cli.get_potenda_from_instance` effective) `except ValueError as exc: print_error_and_abort(f"Failed to initialize the Sync Instance: {exc}")` — so the prefixed abort fires exactly as `cli.py:139-140` today and its `typer.Abort` (what `print_error_and_abort` actually raises — `cli.py:72-74`; exit code 1 with `Aborted.`, NOT `typer.Exit`/exit 0 — E19) propagates out of `execute_run` untouched; every lifecycle failure (including a load-phase `ValueError`) surfaces as today's uncaught traceback of the ORIGINAL type after `execute_run` marks `run.json` failed; a factory `ImportError` is caught nowhere and surfaces as today's uncaught traceback; exit codes and output identical to the current CLI at `9edc1bc` (DBR-009); log lines, `run.json` contents, and exit codes byte-identical to today. Add CLI mapping tests per stage asserting today's exact wording/traceback types (D009): factory `ValueError` → prefixed abort wording + exit code 1 and the `Aborted.` output (`typer.Abort`, E19); a diff-lifecycle failure (incl. load `ValueError`) → uncaught original-type traceback with `run.json` `failed`; factory `ImportError` → uncaught `ImportError` traceback; lock contention → uncaught `filelock.Timeout` traceback. **No adapter module is touched by this task (D012 option A)**: the X5 ergonomic goal — naming `INFRAHUB_ADDRESS`/`INFRAHUB_API_TOKEN` for a missing-credential failure — is satisfied in `run_remote_request`'s sanitized wrap message (T008, contracts/execution-surface.md failure-semantics wrap list) and asserted there (T011), so `infrahub_sync/adapters/infrahub.py` keeps today's wording and DBR-009's byte-identity claim above stays absolute and unqualified. Depends on T007. Trace: DBR-007, DBR-009; DBA-009; contracts/execution-surface.md "Caller obligations" / "Failure semantics"; D009, D012.
- [X] T026 [US4] Refactor the serial branch of `sync_cmd` in `infrahub_sync/cli.py` (the `else` branch taken by `--no-parallel`, or `--parallel` with explicit `order:`) to delegate to `execute_run(sync_instance, operation="sync", confirm_writes=True, print_diff=<--diff>, allow_rowcount_drop=..., continue_on_error=..., potenda_factory=<closure returning the already-constructed engine — see the pinned structure below>, _serial_load_error=lambda exc: print_error_and_abort(str(exc)), _lock_already_held=True, ...)` — the explicit human CLI invocation IS the confirmation; the `_serial_load_error` seam preserves today's inner serial-load handler by construction (D009): `execute_run` marks `run.json` failed, saves, then invokes it, so the unprefixed abort fires at the site exactly as `cli.py:263-268`, and the `typer.Abort` (E19 — exit code 1 with `Aborted.`; a `RuntimeError` subclass, so the broad except does catch it) passes through the preserved outer broad except reproducing `cli.py:285-288`; the `--parallel ignored` warning and the parallel `sync_in_tiers` branch remain in `cli.py` untouched (spec Out of Scope). **Pinned serial/parallel lock-and-engine structure (binding — V1; mirrored verbatim in contracts/execution-surface.md "Already-locked caller (CLI serial sync)")**: unlike `diff_cmd` (T025), `sync_cmd` KEEPS today's outer `with pipeline_lock(sync_instance.name):` block (`cli.py:227`) — the parallel branch still needs it, and the branch predicate `parallel and ptd.tiers` is knowable only once the engine exists. The engine is therefore constructed **EXACTLY ONCE**, in the command body inside that lock, via the T025 CLI wrapper factory (so the prefixed abort still fires at the construction site for both branches); the serial branch then delegates to `execute_run` passing `potenda_factory=<closure that ignores its keyword arguments and RETURNS THE ALREADY-CONSTRUCTED ENGINE>`, so `execute_run` performs no second construction and allocates no second `run_dir`/`run_id` (step 3's pinned seven-kwarg call shape, E23, is still made — the closure discards it). Constructing the engine twice is FORBIDDEN: `utils.get_potenda_from_instance` allocates a fresh `run_dir`/`run_id` whenever `run_id is None` (which `sync_cmd` always passes) and `SyncInstance.compute_order_and_tiers` re-emits its INFO `tier %d (%d): %s` lines — both would break the byte-identity obligation this task inherits from T025 (log lines, `run.json` contents, and exit codes byte-identical to today), which stays absolute. Because the engine already exists inside the caller's lock, `execute_run`'s own step-2 acquisition would self-deadlock in-process (a second same-process `FileLock` on the same path blocks the full timeout, then raises `filelock.Timeout` — probed, E21); the sanctioned avoidance is the CLI-only private seam `_lock_already_held=True` (T007 step 2), which makes `execute_run` skip acquisition for this already-locked caller — never a second `pipeline_lock` wrapper around the `execute_run` call, and never set by `run_remote_request`. The prologue statements after construction (`ptd.force_full_extract = full_extract`, the `run_dir is None` guard, `RunFile(...).save()`, `cli.py:240-245`) stay in the command body exactly as today because the parallel branch needs them; `execute_run` repeating them on the same engine is idempotent (same attribute value, same guard, same `run.json` content at the same path). Same behavior-preservation obligations as T025 — no `__cause__` mapping anywhere; all other lifecycle failures surface as today's uncaught original-type tracebacks. Add CLI mapping tests per stage asserting today's exact wording/traceback types (D009): factory `ValueError` → prefixed abort (exit code 1 + `Aborted.`, E19); serial-load `ValueError` → unprefixed abort (same exit code 1 + `Aborted.`) with `run.json` `failed`; a post-load lifecycle failure (e.g. guardrail `RowcountGuardrailError`) → uncaught original-type traceback with `run.json` `failed`. Depends on T025 (same file). Trace: DBR-007, DBR-009; DBA-009; spec In Scope / Out of Scope; D009.
- [X] T027 [US4] Run the DBA-009 existing targeted test population UNMODIFIED and prove it: `uv run pytest -q tests/test_cli_full_extract.py tests/test_cli_parallel.py tests/cache/test_cli_sync_cache.py tests/test_logging.py` all green, plus `git diff --stat 9edc1bc -- tests/test_cli_full_extract.py tests/test_cli_parallel.py tests/cache/test_cli_sync_cache.py tests/test_logging.py` empty. NOTE (E8): this guarantee depends on execute_run step 7's single-source derivation — the population's `MagicMock` fakes have a no-op `write_plan` and never produce a `plan.parquet`, so any read-back derivation of `summary`/`status`/`changed` would fail these tests; derivation MUST stay on the in-memory row list. The T025/T026 per-stage CLI mapping tests (today's exact wording/traceback types, D009) complement this population for the failure paths the fakes never exercise. Evidence (run report): pytest output and the empty diff. Depends on T025, T026. Trace: DBA-009 (first half); DBR-009; spec informed default (test population); D009.
- [X] T028 [P] [US4] Create `tests/test_no_prefect_import.py` with a **subprocess-isolated probe** (E3/X3 — an in-process `sys.modules` assertion is unsound in the dev+prefect full suite: collection imports `tests/orchestration/test_flow.py`, whose `importorskip` loads prefect, and `--dist loadscope` makes the pollution per-worker arbitrary): the test builds a small script that imports `infrahub_sync`, `infrahub_sync.cli`, `infrahub_sync.execution`, invokes CLI sanity in-process (typer runner: `--help`, `list --directory examples/`), and asserts `not any(m == "prefect" or m.startswith("prefect.") for m in sys.modules)` over ITS OWN interpreter's `sys.modules`; the test executes it in a fresh interpreter via `subprocess.run([sys.executable, "-c", script], ...)` and asserts the exit code and output. Keep the static import-graph check IN-PROCESS (collection-safe): `infrahub_sync/execution.py` imports nothing from `infrahub_sync.orchestration` and nothing in the base package imports `infrahub_sync.orchestration`. Quickstart Scenario 0 / T030 (clean venv) remains the authoritative SC-006 evidence. Depends on T005. Trace: DBA-001; SC-006; DBR-010; contracts/prefect-flow.md §6.
- [X] T029 [P] [US4] Create `tests/test_execution_cli_parity.py`: fixture-level paired comparison using a **behavioral typed `FakePotenda`** — NOT the existing tests' `MagicMock` pattern, whose no-op `write_plan` and `MagicMock` `diff()` never produce a `plan.parquet`, making the fingerprint comparison unsatisfiable (E8): the fake's `write_plan` really writes deterministic rows through `infrahub_sync.cache.parquet_io.write_plan`, and its `diff()` returns a minimal real-shaped diff. Run the CLI `diff` path (typer runner) and `run_remote_request(..., config_directory=..., _potenda_factory=FakePotenda-factory)` — the `FakePotenda` reaches the remote side through `run_remote_request`'s `_potenda_factory` private seam (E18, the sanctioned mechanism; the CLI side passes the same fake as its `potenda_factory` via the usual `infrahub_sync.cli.get_potenda_from_instance` patch) — against reset copies of the same fixture (fresh run ids, unmodified source), then assert equal `status`, `changed`, per-action `summary`, and `compute_plan_fingerprint(cli_run_dir) == compute_plan_fingerprint(remote_run_dir)` via the shared helper (never a reimplementation). Also add the synthetic nested-diff unit test pinning the contract's step-7 caveat (E7): a diff whose only changes sit in nested child elements gives `has_diffs() == True` but zero materialized rows → the result reports `status="no-change"`, `changed=False`, all-zero summary (rows are the preview's result fidelity boundary). Depends on T004, T008, T025. Trace: DBA-009 (second half, automated); SC-007; DBR-007; D009 (step-7 derivation).
- [X] T030 [US4] LIVE verification (DBA-001, SC-006 — quickstart Scenario 0): build a clean venv WITHOUT the extra (`uv venv /tmp/base-venv && uv pip install -p /tmp/base-venv/bin/python .`), run the import + `sys.modules` assertion script, `/tmp/base-venv/bin/infrahub-sync --help`, and `/tmp/base-venv/bin/infrahub-sync list --directory examples/`; `prefect` must not be importable in that venv (any import fails loudly). Evidence (run report): transcript showing all commands succeed and the assertion passes. Depends on T025, T026, T028. Trace: DBA-001; SC-006; DBR-010.
- [X] T031 [US4] LIVE verification (DBA-009 paired comparison, SC-007 — quickstart Scenario 4): on reset copies of the qualified fixture (destination returned to zero `InfraDevice` objects before each compared run, fresh run ids, MockDB unmodified), run `uv run infrahub-sync diff --name custom-example --directory examples/` and one remote `operation=plan` run; compare `status`, `changed`, per-action summary, and `compute_plan_fingerprint` over both run directories. Evidence (run report): both run-dir paths, both `run.json` files, the fingerprint-equality output. Live-environment ceiling fallback per R-3. Depends on T019, T025, T029. Trace: DBA-009; SC-007; DBR-007, DBR-009.

**Checkpoint**: Seam complete with three real callers; base install provably Prefect-free;
CLI ≡ remote parity proven.

---

## Phase 7: User Story 5 — Reproducible example + docs (Priority: P5)

**Goal**: One shipped example lets a developer unfamiliar with the implementation
install, load the schema, configure, serve, remotely invoke, and inspect the preview
without reading package source.

**Independent Test**: A clean-context walkthrough following only
`examples/prefect_remote_run/README.md` reproduces the qualified plan demonstration
(quickstart Scenario 6; DBA-002/011).

- [X] T032 [P] [US5] Author `examples/prefect_remote_run/schemas/infra_device.yml` — a loadable Infrahub schema YAML defining `InfraDevice` with attributes `name` and `type` (matching the `examples/custom_adapter/config.yml` schema_mapping and the lab schema per R-3; avoid the deprecated `display_labels`/`default_filter` keys the lab warned about); include the load command in the README (T034). This is the R-3 enabling work — no loadable `InfraDevice` schema YAML ships in the repository today. Trace: R-3; DBR-013; DBA-002; spec In Scope (schema authoring).
- [X] T033 [P] [US5] Author the REST request corpus under `examples/prefect_remote_run/requests/`: documented request bodies (JSON files with the target endpoint noted) for at least (1) create a plan flow run, (2) create a confirmed sync flow run, (3) get flow-run state (`GET /api/flow_runs/{id}`), (4) read run logs (`POST /api/logs/filter` with `flow_run_id` filter) — matching `contracts/prefect-flow.md` §5 exactly. Placeholder convention (binding; T036 scans against it): every credential-shaped value is a fixed angle-bracket sentinel — `<your-api-token>` for token values (e.g. `INFRAHUB_API_TOKEN=<your-api-token>`, `Authorization: Bearer <your-api-token>`), `<your-infrahub-address>` for endpoint values — never a realistic-looking string; the same sentinels are used verbatim in the README (T034) so T036's scan is deterministic. These bodies are the corpus DBA-008 scans. Trace: DBR-011, DBR-013; DBA-008 (corpus definition); US5 acceptance scenario 2.
- [X] T033a [P] [US5] Fixture diagnosability for the qualified example (X1/X14) — **RATIFIED (D013, Blake Ellis, 2026-07-30)**: this task changes a SHIPPED example file, so it was a gate item rather than an inherited allowance, and the human ratified it. Authority: **DBA-011** (a clean-context stranger must be able to reproduce the demonstration — the confirmed X1 hazard means a served fixture can silently produce a wrong answer for the brief's own qualified demonstration, which makes the demonstration unreliable rather than merely unpolished) and **DBR-012/DBA-004** (adapter narration must reach the Prefect run log to be remotely observable). It does NOT rest on the brief's conditional fixture-repair allowance, whose trigger condition (the fixture-compatibility assumption proving wrong) never occurred — R-3's smoke test produced the expected five creates. The five-device outcome is UNCHANGED by both halves; they are observability-only. (1) `MockDBClient` in `examples/custom_adapter/custom_adapter_src/custom_adapter.py` logs a WARNING through the `infrahub_sync` logger hierarchy (a logger name the DBR-012 bridge covers, e.g. `logging.getLogger("infrahub_sync.examples.custom_adapter")`) when a configured `db_path` does not exist — turning the silent empty-plan wrong answer into a remotely visible diagnosis; the five-device outcome of the intact fixture is unchanged; (2) replace the example adapter's narration `print("DEBUG: ...")` calls with `logging` calls under the same bridged hierarchy, so the mockdb lifecycle ("Loading 5 InfraDevice nodes") appears in the Prefect run log the example itself demonstrates (X14); regenerate/verify the derived `examples/custom_adapter/mockdb/` adapter stays consistent. Depends on Phase 2 (logger-hierarchy convention); independent of T034 content. Trace: DBA-011 (clean-context reproducibility of the walkthrough), DBR-012 + DBA-004 (bridged remote observability of adapter narration); X1/X14 remediation; D013 (gate item — the example's user-visible narration mechanism changes, so the human ratifies it like D007/D011).
- [X] T034 [US5] Author `examples/prefect_remote_run/README.md`: explicit machine prerequisites (Python 3.10–3.13, the pinned `prefect==3.8.1` extra, a reachable Infrahub with the `InfraDevice(name, type)` schema, an EMPTY destination — zero `InfraDevice` objects — with a short verify command and a pointer to the reset procedure, X11; port 4200 free for the Prefect server with a one-line note on what a collision looks like, X11; and the WORKING DIRECTORY: the serve process and any local CLI comparison run start from the repository root — stated as a prerequisite line AND repeated next to the serve command, since the example config's `./` paths and the default cache root resolve against the serving process's CWD, X1/E11); install instructions that use a preview-accurate install source — installing from the repository checkout at this branch (e.g. `pip install -e '.[prefect]'` from the repo root, or an equivalent local-path/built-wheel command), NOT `pip install 'infrahub-sync[prefect]'` from PyPI — with the README stating why: the preview exists only on this branch and is not published to PyPI, and the T037 clean-context walkthrough must succeed following only the README; schema loading using `schemas/infra_device.yml`; runner configuration (`INFRAHUB_SYNC_CONFIG_DIRECTORY="$PWD/examples/custom_adapter"` with the one-sentence scoping guidance — point the variable at a directory containing only the configurations you intend to expose remotely, X6; credentials via `INFRAHUB_ADDRESS`/`INFRAHUB_API_TOKEN` runner-environment variables only, set before starting serve, values shown as the T033 placeholder sentinels); `prefect server start`; `python -m infrahub_sync.orchestration.serve` (from the repo root); remote invocation and inspection walking through the `requests/` corpus INCLUDING the expected checkpoint output — the `Loading 5 InfraDevice nodes` narration and the five-creates plan summary (`summary=create:5,update:0,delete:0`) — so an empty plan is recognizably wrong (X1); one sentence that `artifact_path` is on the runner host (run artifacts are runner-local, not fetchable through Prefect, X11); a "stopping and cleaning up" section (stop serve, stop the server, where `PREFECT_HOME`'s SQLite and the `.infrahub-sync-cache/` run directories live, X11); the MANDATORY trusted-development-environment caveat (the default self-hosted server must never be exposed to the public internet); no real credentials anywhere. Depends on T032, T033 (and functionally on Phase 3). Trace: DBR-013; DBA-011; SC-009; US5 acceptance scenarios; spec Constraints (caveat ownership).
- [X] T035 [US5] Add one reference docs page `docs/docs/reference/prefect-remote-run.mdx` for the optional Prefect preview (plan: "one reference page"; governance-origin scope per **D011** — AGENTS.md Documentation mandates docs for user-visible changes; RATIFIED at the checkpoint gate, Blake Ellis, 2026-07-30): the optional extra's single pin `prefect==3.8.1` and the base-dependency change it requires (`diffsync[redis]>=2.1,<3.0` → `diffsync>=2.1,<3.0` + `redis>=4.3,<9`, one line of D005-option-D rationale each: the `[redis]` extra's `redis<5.0` cap collides with prefect's `pydocket` → `redis>=5`, `redis` is declared directly because `utils.py` imports `RedisStore` unconditionally, and the floor stays permissive so `diffsync[redis]` consumers still resolve) — note for readers upgrading an existing install that the redis client is now a direct dependency with a wider allowed range, and do NOT document the withdrawn D006 companion pins, the flow's four parameters and defaults, `INFRAHUB_SYNC_CONFIG_DIRECTORY`, the `RunResult` fields, the confirm-writes gate, the summary log line as the supported remote observation surface with its FIXED key=value format `summary=create:N,update:N,delete:N` and the note that its fields mirror `RunResult` (X7), the four-row status-mapping table (Prefect state · RunResult status · run.json status · meaning — e.g. COMPLETED · planned · dry-run · "plan with changes"; facts from data-model, X13), the trusted-environment caveat, and a pointer to `examples/prefect_remote_run/`; register the page in `docs/sidebars.ts`; REVISE the Prefect subsection of `docs/docs/orchestration.mdx` to LEAD with the packaged optional integration (one short paragraph + link to the new reference page and example), demoting the existing hand-wrap-the-CLI guidance to "if you need behaviors the preview doesn't cover (schedules, retries, work pools)" — a cross-link alone would leave the obsolete DIY pattern primary (X9); lint with `uv run rumdl check .`. Depends on T034. Trace: DBR-013 (user-facing docs governance — AGENTS.md Documentation); DBR-003, DBR-015 (documented contract); D005 (option D), D011 (D006 superseded — nothing to document).
- [X] T036 [US5] Example hygiene scan (DBA-008 corpus half + completion condition): scan `examples/prefect_remote_run/` (README, `requests/`, `schemas/`) proving no real credential, no seeded canary value, and that every credential-shaped string follows the T033 placeholder convention; implemented as a standalone automated test `tests/test_example_hygiene.py` with NO prefect dependency (no `importorskip`; runs and passes in a base install) that greps the example tree for the canary set and for credential-shaped strings not matching the T033 sentinels. Depends on T033, T034. Trace: DBA-008; SC-005; DBR-006; spec Completion Conditions ("The example contains no real credentials").
- [X] T037 [US5] LIVE verification (DBA-011, SC-009): a clean-context walkthrough — an operator or fresh agent session with no exposure to the implementation — follows ONLY `examples/prefect_remote_run/README.md` on a machine meeting its prerequisites and reproduces the qualified plan demonstration (server + served deployment start from the documented commands, remote plan of five creates, state and log inspection), with zero references to package source. Evidence (run report): the walkthrough transcript, an attestation that only the README was consulted, and the reproduced plan summary. Live-environment ceiling fallback per R-3. Depends on T034, T018. Trace: DBA-011, DBA-002; SC-009; DBR-013.

**Checkpoint**: Example reproduces the preview end-to-end for a stranger.

---

## Phase 8: Polish, governance gates, and delivery evidence

**Purpose**: Repository-governance gates (format → lint → ty → tests → CLI sanity →
docs), scope audit, and collated traceability evidence.

- [ ] T038 [P] Scope audit (DBR-014): inspect the final diff (`git diff 9edc1bc --stat` and the new modules) confirming none of backlog B-001–B-007 shipped — no custom HTTP service or Sync-shaped REST resource model, no saved-plan browse/approve/apply-by-run-ID remote workflow, no per-stage Prefect tasks, no work pools/workers/retries/PostgreSQL/Redis-for-prefect/Kubernetes (note for the auditor: the base `redis>=4.3,<9` declaration is D005 option D — the client diffsync's store already required, declared directly now that the `[redis]` extra's cap is gone — NOT a Redis backing service for Prefect), no schedules/triggers/notifications, no production auth/HA claims, no custom operator UI, no configuration registration/versioning, prefect absent from base `dependencies`, parallel `sync_in_tiers` branch untouched. Record the audit in the run report. Trace: DBR-014; spec Out of Scope; Completion Conditions ("No backlog item implemented").
- [ ] T039 Run the required workflow on the final tree, with the **corrected inherited lint baseline** per **D014** (RATIFIED, Blake Ellis, 2026-07-31): (1) `uv run invoke linter.format` — the Python formatters alone, expecting **no Python-formatter diffs**; use this and **NOT** `uv run invoke format`, because `invoke format` runs `rumdl fmt`, which CORRUPTS Markdown (it misparses a wrapped paragraph line beginning `#3 (…)` as a heading, loses text, and cascades a heading demotion through the rest of the file) — never run `rumdl fmt` over this spec directory. (2) `uv run rumdl check .` — must be **clean**; that part IS achievable and this run must not regress it. This assertion is load-bearing for the next one: `invoke lint` runs `rumdl` FIRST with no `warn=True`, so any Markdown lint issue anywhere aborts the chain before ruff/pylint/yamllint/ty run at all, and the pylint assertion below is meaningless unless rumdl is clean. (3) `uv run invoke lint` — expect **exit 30, NOT exit 0**, with exactly the inherited pylint diagnostic set (29 × `C0415` `import-outside-toplevel` across six modules — `__init__.py`, `dependency_graph.py`, `cli.py`, `utils.py`, `cache/`, `potenda/__init__.py` — plus 1 × `E0213` `no-self-argument` at `infrahub_sync/__init__.py:98` (`convert_str_to_enum` should take `self`), rating **9.60/10**) and **NO new pylint diagnostics attributable to this run**. The gate is therefore **"no regression against the inherited baseline"**, not "exit 0". Proven inherited: `pylint infrahub_sync/` is pylint's only target (`tasks/linter.py:41`) and `git diff main..HEAD -- infrahub_sync/` is empty. Because pylint's non-zero exit propagates, the chain stops there, so the remaining legs are asserted by direct invocation: `uv run invoke linter.lint-yaml` clean, and (4) `uv run ty check .` (exit 0, no `[[tool.ty.overrides]]` added, any targeted `# ty: ignore[<rule>]` carries a TODO). **The ty gate runs in the dev+prefect-synced environment** (`uv sync --extra dev --extra prefect` — E9: without prefect installed, the `orchestration/` modules produce unresolved-import diagnostics), and that environment produces one EXPECTED new diagnostic that must be recorded rather than fixed: installing prefect pulls in `prometheus-client`, which resolves the optional import at `infrahub_sync/adapters/prometheus.py:10` and therefore makes its `# ty: ignore[unresolved-import]` report `warning[unused-ignore-comment]`. That comment remains NECESSARY in a base/no-prefect environment (where the import genuinely does not resolve), so **the adapter file is NOT edited — that is out of scope for this run**. Gate expectation, stated so a later reader cannot mistake it for a regression: the diagnostic set is exactly the inherited `unused-ignore-comment` directives (in `tests/adapters/test_nautobot_incremental.py`) PLUS exactly one `unused-ignore-comment` at `infrahub_sync/adapters/prometheus.py:10`, cause named as prefect→prometheus-client; the baseline count moves from 4 to 5 as recorded at the gate (a dev-only sync on this worktree measures 3 inherited, so the binding assertion is the SET, and T039 records the absolute numbers it observes in the run report). All are `warning` severity, so `ty check` still exits 0; no new `error` diagnostics are permitted. New/changed code is Ruff-clean and fully typed with docstrings; the two documented D009 broad-except sites each carry an explanatory comment, and their suppression directives follow the mechanism-conditioned E16 rule (contracts/execution-surface.md "BLE001 suppression rule") — the gate is satisfied in exactly this configuration: `invoke lint` reaching the inherited-baseline exit 30 with **zero ruff findings** (ruff runs before pylint, so its leg is asserted) **WITH** the targeted `# noqa: BLE001` present on `run_remote_request`'s `except Exception` line (every E5-permitted chain mechanism there fires BLE001 — probed: `... from None` and `... from RuntimeError(str(exc))` both fire; plain `from exc` is clean but is the leak E5 forbids) and **WITHOUT** any directive at `execute_run`'s step-6 site (blind `except` + bare `raise` does not fire BLE001, so a directive there would report `RUF100 Unused noqa directive` and fail this very gate). Neither directive is added or omitted speculatively: what `uv run ruff check` reports for the code as written is the arbiter. Unchanged inherited baselines for cross-reference: `uv run pytest -q` → 111 passed, 2 skipped; `uv run ty check .` → 3 diagnostics, exit 0. Trace: R-4; AGENTS.md Required Development Workflow + policy; spec Constraints; Constitution IV; D009; D014.
- [ ] T040 Run the full suite `uv run pytest -q` in the dev+prefect environment: no regression versus the 110-passed/3-skipped baseline (gate-verified reference point: under the upgraded dependency set alone — before this run's new tests — the inherited population reports 111 passed / 2 skipped with zero failures, the same 113-test total as the baseline; final counts additionally include the tests added by this run, T012a among them) (all inherited tests still pass; new unit tests pass; prefect-dependent tests skip cleanly when the extra is absent — spot-check with a base-only env if practical; live integration tests remain opt-in via `-m integration`). Record final counts in the run report. Depends on all test tasks. Trace: R-4, R-5; SC-006 (baseline clause); Constitution V.
- [ ] T041 CLI sanity on the final tree, all three commands run: (a) `uv run infrahub-sync --help` and (b) `uv run infrahub-sync list --directory examples/` (the new `examples/prefect_remote_run/` must not break discovery — it ships no `config.yml`, only README/schemas/requests) — for (a) and (b) assert `git status --porcelain` is empty, since neither command writes; (c) `uv run infrahub-sync generate --name from-netbox --directory examples/`, which **does not leave the tree clean and is not expected to** per **D014** (RATIFIED, Blake Ellis, 2026-07-31). Expected outcome of (c): ~242 changed lines across 4 files under `examples/netbox_to_infrahub/`, pure ordering churn at two levels (generated model-block order, and member order inside `_attributes` tuples) with **no content loss** (20 models and 20 classes before and after, matching attribute multisets). The task must **RESTORE** that churn — `git checkout -- examples/netbox_to_infrahub/` — and must **NOT** commit it. Cause, measured and not to be re-derived: `generate` renders from the live Infrahub schema and `infrahub_sync/generator/templates/diffsync_adapter.j2:26` and `diffsync_models.j2:29` both iterate `schema.items()` unsorted, so emitted order tracks the API response order. A fixed point **does** exist — three consecutive runs are byte-identical to each other across all four generated files (verified pairwise, run1 == run2 == run3) — but it **moves**: the committed T002 output at `e04f262` differs from today's stable output by 484 lines across 4 files (22 + 220 + 22 + 220). The order is therefore **stable within a window and shifts across windows**, which is why this churn is **not eliminable by committing a snapshot**: a committed snapshot yields a clean tree only until the order shifts again, at which point a clean-tree gate silently starts failing for someone who changed nothing. Cause of the shift is **unverified** — most plausibly a cold-vs-warm server schema cache or a schema reload; not tested, because that would require restarting the lab containers, out of bounds for this run. (Option B, "commit the current fixed point", is **viable, not impossible**; it was re-offered to the decision-maker and **declined** under D014 in favour of C, because a gate that passes today and fails later without anyone touching the generator hides itself, whereas C never pretends the defect is fixed.) This is an **INHERITED** defect, not one this run introduced — proof: `git diff main..HEAD -- infrahub_sync/generator/` is empty, so the behavior comes from `main`. No known correctness impact (diffsync matches attributes by name, and this feature's canonical plan fingerprint per D001 sorts plan rows explicitly, so it is order-insensitive). State it plainly in the evidence: this is a **pre-existing baseline failure reported rather than hidden**, per the brief's completion condition and per D014, and the fix (sorting in the generator, then regenerating all checked-in examples) is **deliberately deferred to a separate PR**, tracked as `bug-generate-output-is-not-deterministic-across-runs.md` in the planning repo's proposed-issues directory. Evidence: transcripts of (a), (b), (c) in the run report, the observed changed-line count and file list from (c), the restore command, the D014 reference, and the tracked issue name. Trace: R-2; DBR-009; D014; AGENTS.md CLI sanity; spec Constraints; spec Completion Conditions (baseline-failure reporting clause).
- [ ] T042 Collate delivery evidence in `dev/specs/001-prefect-managed-remote-run/run-report.md`: the full DBR-001–015 / DBA-001–011 traceability table below with per-item evidence pointers (test names, transcript sections); the R-4/R-5 inherited-baseline facts reported (not hidden); and — if any of T018/T019/T021/T024/T031/T037 could not run against the live environment — the live-environment ceiling record naming the affected criteria (at most DBA-002–005, DBA-008, DBA-009's paired comparison, DBA-011) with the substitute local evidence, per the spec's informed default. Depends on all prior tasks. Trace: spec Completion Conditions; R-3, R-4, R-5.

---

## Decision record D007 (this phase) — RATIFIED (D007, Blake Ellis, 2026-07-30)

- **Question**: R-1 mandates changing `uv sync` → `uv sync --extra dev` in `AGENTS.md`,
  committed alone as commit 1. `.github/copilot-instructions.md` carries the same two
  `uv sync` lines (AGENTS.md's Platform-Specific Notes require platform files to include
  the Required Development Workflow block *verbatim*). Does commit 1 include the mirror
  file, or does it drift?
- **Evidence**: `grep -n "uv sync" .github/copilot-instructions.md` → lines 18 and 26,
  identical to the AGENTS.md lines R-1 names; AGENTS.md "Platform-Specific Notes"
  section mandates verbatim inclusion; the brief's R-1 rationale is "so every later
  agent reading repository instructions gets the working command" — which includes
  Copilot agents reading the mirror. No `.cursor/rules/dev-standard.mdc` exists in this
  worktree.
- **Options**: (1) Update only `AGENTS.md` — literal R-1, but leaves the repo violating
  its own verbatim-mirror rule and later Copilot agents with the broken command;
  (2) Update `AGENTS.md` and `.github/copilot-instructions.md` together in commit 1 —
  one logical change, both instruction surfaces consistent; (3) Update the mirror in a
  separate later commit — splits one logical change and leaves a window of drift.
- **Recommendation**: Option 2 (encoded in T001).
- **Rationale**: R-1's purpose (working commands for every instruction-reading agent)
  and AGENTS.md's own verbatim-mirror governance are both satisfied; commit 1 remains a
  single isolated logical change ("fix the documented sync command"), which is the
  plain reading of "committed alone".
- **Confidence**: High.
- **Origin**: governance (repository AGENTS.md Platform-Specific Notes) intersecting a
  brief-mandated task's literal file list; no product scope affected.

---

## Traceability coverage (every DBR/DBA → owning tasks)

| ID | Owning tasks (implementation → verification) |
|---|---|
| DBR-001 | T014 → T017, T018, T019 |
| DBR-002 | T015 → T016, T018 |
| DBR-003 | T008, T014 → T011 (branch passthrough), T016, T019, T020, T021 |
| DBR-004 | T007 → T011, T020, T023, T024 |
| DBR-005 | T006, T008, T014 → T011, T023, T024 |
| DBR-006 | T005, T008, T014 → T022, T033, T036 |
| DBR-007 | T005–T008, T014, T025, T026 → T029, T031 |
| DBR-008 | T014 (in-process call) → T011 (subprocess spies), T024 |
| DBR-009 | T025, T026 → T027, T031, T041 |
| DBR-010 | T012, T013 → T028, T030 |
| DBR-011 | T014 → T017, T019, T033 |
| DBR-012 | T014 (bridge) → T016, T019, T022 |
| DBR-013 | T032, T033, T034, T035 → T037 |
| DBR-014 | T012 (base deps: only the D005-option-D `diffsync[redis]` → `diffsync` + `redis` swap; no backlog item, engine untouched) + T012a (redis path really exercised) → T038 |
| DBR-015 | T005, T007 → T010, T011, T020, T023 |
| DBA-001 | T028, T030 |
| DBA-002 | T015, T032 → T018 (live), T037 |
| DBA-003 | T014 → T017, T019 (live) |
| DBA-004 | T014, T016 → T017, T019 (live) |
| DBA-005 | T020 (automated analog) → T021 (live) |
| DBA-006 | T011 (spies) → T023, T024 (live, destination observation) |
| DBA-007 | T011 (parametrized negatives + spies) → T024 (live) |
| DBA-008 | T022 (canary scan: params/results/logs) + T036 (example request-body corpus) + T024 (live REST-transcript grep) |
| DBA-009 | T027 (population unmodified) + T029 (automated parity) + T031 (live paired comparison) |
| DBA-010 | T010, T011, T020 (result schema; validation + execution faults) + T023 (flow-level) |
| DBA-011 | T033a (fixture diagnosability), T034 → T037 (live clean-context walkthrough) |

Decision IDs (full D001–D013 → artifact map in plan.md; **all RATIFIED at the checkpoint
gate by Blake Ellis on 2026-07-30** — D005 as option D, an override of its recorded
recommendation, with D006 superseded as a consequence): D001 (fingerprint —
T004/T009), D002 (config directory — T015/T016/T018), D003 (engine defaults + lock —
T007/T008), D004 (log bridge + level ownership — T014/T016), D005 option D
(`prefect==3.8.1` extra + base `diffsync>=2.1,<3.0` & `redis>=4.3,<9` — T012, T012a,
T035, T039), D006 SUPERSEDED/WITHDRAWN (no companion pins — nothing to implement),
D007 (T001), D008 (tasks header logging convention), D009 (wrap boundary —
T007/T008/T025/T026/T027; contracts/execution-surface.md "Failure semantics"),
D010 (tolerant resolution — T006/T011), D011 (docs page governance scope — T035),
D012 (missing-credential env-var naming at the remote wrap, adapters untouched —
T008/T011/T025), D013 (T033a shipped-example diagnosability as a gate item — T033a).
Spec clarifications: #1 (D001) → T004/T009; #2 (D002) → T015/T016/T018; #3 (D003) →
T007/T008; #4 (D004) → T014/T016.
Enabling work: R-1 → T001; R-2 → T002, T041; R-3 → T019/T021/T024/T031/T037 fallback +
T032; R-4 → T003, T039, T040; R-5 → T003, T040.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 (Setup)**: T001 → T002 → T003 (strict; commits 1 and 2 are bound by the plan).
- **Phase 2 (Foundational)**: after Phase 1. T004 ∥ T005; then T006 → T007 → T008;
  T009 after T004; T010 after T005; T011 after T008 and T010. BLOCKS all user stories.
- **Phase 3 (US1)**: after Phase 2. T012 ∥ T013; T012a after T012; T014 after T008+T012+T013; T015 after
  T014; T016/T017 after T015; T018 after T015 (live); T019 after T018 (live).
- **Phase 4 (US2)**: T020 after T007 (can start with Phase 3); T021 after T019 (live,
  strictly sequential runs).
- **Phase 5 (US3)**: T022/T023 after T016; T024 after T018 (live).
- **Phase 6 (US4)**: T025 after T007; T026 after T025; T027 after T026; T028 after T005;
  T029 after T004+T008+T025; T030 after T028+T026; T031 after T019+T029 (live).
- **Phase 7 (US5)**: T032 ∥ T033 anytime after Phase 1; T033a after Phase 2 (logging
  convention), before T037 (the walkthrough exercises the diagnosable fixture);
  T034 after T032+T033 (content finalized only once Phase 3 commands are real);
  T035 after T034; T036 after T034; T037 after T034+T033a+T018 (live).
- **Phase 8 (Polish)**: after all desired stories. T038 ∥ early; T039 → T040 → T041 →
  T042 last.

### Live-environment chain (lab Infrahub + Prefect server; R-3)

T018 → T019 → T021 → T024 → T031 → T037 run against the live lab; each records its
evidence in the run report; any unavailability is recorded as the live-environment
ceiling (T042), never silently skipped. The three qualified-demonstration runs
(plan → confirmed sync → follow-up plan) are strictly sequential (spec edge case 5).

### Parallel Opportunities

```text
Phase 2: T004 ∥ T005 (different files); later T009 ∥ T010 ∥ (T028 from US4)
Phase 3: T012 ∥ T013 (pyproject vs new package file); T012a ∥ T013/T014 (own test file)
US5 prep: T032 ∥ T033 can run any time after Phase 1, alongside Phases 2–6
Polish:  T038 can run alongside the T039–T041 gate sequence
```

---

## Implementation Strategy

### MVP first (US1)

1. Phase 1 (commits 1–2 + baseline) → Phase 2 (surface + fingerprint, unit-tested)
2. Phase 3 (extra, flow, serve, tests, live plan demonstration) — **stop and validate**:
   quickstart Scenarios 0-adjacent unit tests plus Scenario 1 live evidence = MVP.

### Incremental delivery

- US2 adds the confirmed write + convergence (Scenario 2 live).
- US3 adds the safety/canary verification set (Scenario 3 live).
- US4 lands the CLI refactor last among code changes so the DBA-009 population and
  parity checks run against the finished surface (Scenarios 0 and 4).
- US5 packages everything as the example + docs (Scenario 6), then Phase 8 gates and
  the collated evidence report close the brief's completion conditions.

### Notes

- Never modify the DBA-009 test population files; T027 proves it with a diff.
- `infrahub_sync/orchestration/flow.py` must NOT use `from __future__ import
  annotations` (research F3); `infrahub_sync/execution.py` may.
- No secrets in code, examples, logs, or evidence transcripts; canary discipline per
  DBA-008. Credentials only via runner environment (`INFRAHUB_ADDRESS`,
  `INFRAHUB_API_TOKEN`), re-derived on the lab host per R-3 if absent.
- No git operations are performed by task execution itself; the root orchestrator owns
  all commits at the marked boundaries.
