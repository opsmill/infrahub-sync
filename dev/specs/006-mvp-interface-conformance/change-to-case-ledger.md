# Change-to-case ledger

| Change | Red matrix case | Repair | Green evidence |
|---|---|---|---|
| Add configured standalone DB-003 projection | C01: configured standalone plan left no `ProductRun` or `plan-review`; initial run failed collection with `ModuleNotFoundError: infrahub_sync.product_store.standalone` | Add `execute_standalone`, create unfinished plan/sync records, publish the existing review shape, finish only after complete publication | `test_configured_standalone_plan_publishes_the_managed_product_contract` |
| Preserve D028 identity through reviewed apply | C02: standalone apply had no product record to extend | Require the original record in the same cache and finish it under the original run ID | `test_configured_standalone_apply_extends_the_planning_record` |
| Keep one confirmed-sync identity | C03: public composed sync had three core calls but no durable product owner | Pass the semantic operation through plan/verify/apply and defer completion until apply | `test_python_confirmed_sync_keeps_one_product_identity_across_all_stages` |
| Route both standalone interfaces through one adapter | C04: CLI and public Python had no product-cache configuration axis | Add optional absolute `product_cache_location` request field and matching CLI option; both call `execute_standalone` | `test_python_plan_request_projects_the_same_record_and_review_artifact`; `test_cli_plan_accepts_explicit_product_cache_configuration` |
| Prove the managed/standalone product seam | C05: no executable comparison retained every DB-003 field | Compare observed complete plan records and exact review bytes while normalizing only generated record timestamps; operation-envelope equality remains owned by the executed matrix | `test_managed_and_standalone_plan_product_projection_seams_match`; oracle mutation tests |
| Retain lifecycle and secret safety | C06: standalone projection failure/restart behavior was unproved | Reuse DB-003 atomic/redaction boundaries; retain typed error class only and never persist exception text | restart and secret-sentinel conformance tests |
| Exact-path normalization | C07: recursive key-name normalization hid nested semantic `run_id` and `created_at` disagreements | Replace it with an exact schema-path allowlist | `test_oracle_does_not_normalize_semantic_payload_keys` |
| Executable operation matrix | C08: the initial oracle compared only direct standalone and managed plan observations | Add explicit executed-surface adapters; invoke CLI/Python/managed worker for plan/apply/sync, Python/managed verify, CLI same-byte review, and HTTP submission through worker completion for all four stages | `tests/conformance/test_interface_matrix.py` |
| Preserve semantic managed sync operation | C09: managed confirmed sync retained the apply substage operation | Override only the composed result operation to `sync` | `test_managed_confirmed_sync_retains_the_semantic_sync_operation` and sync matrix |
| Retain partial apply evidence | C10: standalone apply failure omitted `ApplyRecord` fields | Reuse the managed `ApplyRecord.as_summary_keys()` projection | `test_configured_apply_failure_retains_partial_write_evidence` |
| Publish direct-sync review before writes | C11: configured CLI sync projected its review only after core completion | Add one private plan-commit callback at the serial and tiered pre-write boundaries | serial/parallel pre-write ordering and publication-refusal tests |
| Type duplicate configured plans | C12: DB-003 duplicate-run rejection escaped the CLI boundary | Translate it to the existing standalone typed refusal | `test_duplicate_configured_cli_plan_is_a_one_line_typed_refusal` |
| Keep CLI review read-only | C13: the expanded matrix exposed configured review falling into the sync-result branch | Handle non-verifying review as a SavedPlan with no product mutation | same-byte CLI review matrix |
| Derive the oracle from observed boundaries | C14: the first adapters recomputed operation/counts/outcome from plan/product inputs and accepted literal destination counts | Map captured CLI core plus rendering, public `RunResult`, and managed worker dictionaries; measure an instrumented nonzero destination | `test_interface_adapter_mutations_cannot_false_pass`; nonzero plan/apply/sync matrix; HTTP worker-envelope assertions |
| Retain terminal evidence after partial publication | C15: a failure after artifact reservation prevented the ProductRun failure transition | Permit only a failed terminal transition while an artifact is pending; keep successful completion blocked and the artifact unavailable | `test_configured_sync_mid_publication_failure_is_terminal_and_artifact_stays_incomplete`; `test_interrupted_publication_can_resume_and_finish_the_run` |
| Type unresolvable user-cache paths | C16: `Path.expanduser()` raised raw `RuntimeError` for an unknown `~user` | Translate standalone preparation to `StandaloneProductRecordError` and API request validation to `ValidationError` | CLI unresolvable-user refusal and `test_requests_are_strict_and_immutable` |
| Isolate real core lock acquisition | C17: the pre-write ordering test could acquire its real pipeline lock beneath the checkout | Set a per-test absolute `INFRAHUB_SYNC_CACHE_DIR` under `tmp_path` | serial/parallel `test_configured_cli_sync_publishes_review_before_any_destination_write` |
| Enforce one owner before run-ID normalization | C18: independently normalizing run IDs could hide wrapper ownership disagreement | Validate all schema-owned run identities within each envelope before cross-surface replacement | `test_oracle_refuses_intra_envelope_run_ownership_disagreement` |

No production edit exists without a case above. Existing managed lifecycle ownership and
DB-005 routes were not refactored.
